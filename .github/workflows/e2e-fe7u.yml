name: "E2E: FE7U"

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  checks: write

jobs:
  e2e:
    uses: ./.github/workflows/e2e-run.yml
    with:
      rom-name: FE7U
      job-name: "E2E FE7U"
      timeout: 15
    secrets: inherit
