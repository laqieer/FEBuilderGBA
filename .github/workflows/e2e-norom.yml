name: "E2E: No ROM"

on:
  schedule:
    - cron: '3 20 * * *'   # Daily UTC 20:03 (Beijing 04:03)
  workflow_dispatch:

permissions:
  contents: read
  checks: write
  issues: write

jobs:
  e2e:
    uses: ./.github/workflows/e2e-run.yml
    with:
      rom-name: none
      job-name: "E2E No ROM"
      timeout: 10
      caller-event: ${{ github.event_name }}
    secrets: inherit
