# Core-Seam Catalog

This file holds the full per-file **Core-seam catalog** that was relocated out of
`CLAUDE.md` to keep that file under the ~40,000-char harness truncation limit
(see issue #1645; regression of the earlier #1039 trim). Each entry documents one
cross-platform `FEBuilderGBA.Core` class (or Avalonia ViewModel seam) ported from
a WinForms form, with its behavioural contract and the issue that introduced it.

`CLAUDE.md` keeps the high-value architecture overview and a pointer here; this
file is the exhaustive reference. New seam entries belong **here** (one paragraph
each), NOT in `CLAUDE.md`.

## #1978 Review-Hardening Corrections

These corrections supersede the older wording in the detailed random-map entries
later in this catalog:

- `BuiltInRandomMapTilesetCore` now rejects any PLIST table-base pointer slot that
  does not have all four bytes in the ROM and requires the complete fixed
  512-byte palette snapshot; truncation is a resolution failure, never a shorter
  fingerprint input.
- `BuiltInRandomMapEdgeSignatureCore` follows the accepted Plan v4 sparse-corpus
  contract: it compares raw, palette-independent 4bpp boundary indices after TSA
  hFlip/vFlip. Palette-bank bits do not change structural edge compatibility.
- `BuiltInRandomMapCorpusCore.TryBuildCorpus` accepts a cancellation token and
  checks it during map, row, and cell scanning; `TryGenerateFromRom` converts
  cancellation into typed `BuiltInRandomMapErrorCategory.Cancelled`.
- Fingerprinted malformed FEMapCreator mappings survive load/save so `Lookup`
  reports `Invalid` instead of silently degrading to `NoMapping`. Backend
  selection carries typed mapping status/reason only; Avalonia localizes the
  stale/invalid notice at the UI boundary.
- One-click FEMapCreator profile/mapping resolution runs off the UI thread.
  Authoritative executable, image, and generation-data hashes use bounded reads
  that observe the generation cancellation token. The three required Config
  strings are snapshotted before the worker hop; the background resolver never
  reads the live UI-mutated Config dictionary.
- The Map Editor has an inline generation Cancel button, materializes blank seeds
  before backend invocation, and passes cancellation into the dispatched apply
  callback before undo/ROM mutation. "Map Tileset..." selects Options' External
  Tools tab and brings the FEMapCreator section into view.
- The Options executable-identity cache is live-status-only. Explicit discovery
  and Save Mapping use uncached authoritative hashes, so a same-size executable
  replacement with preserved mtime cannot create an immediately stale mapping.
- One-click built-in generation clones the ROM and current grid before its first
  worker hop. Corpus resolution therefore reads an immutable point-in-time
  snapshot while apply-time live-ROM identity/fingerprint checks remain mandatory.
- Options displays the Built-in Experimental visual-coherence-only disclaimer;
  success status names the exact `Built-in Experimental` or
  `FEMapCreator Experimental` backend. The setup wizard opens Options and invokes
  the no-fingerprint `ShowFEMapCreatorSection` initializer.

## Graphics-System Core Seams

The `ImageUtil*.cs` family plus the LZ77/battle/skill/map/world-map/event/song
Core seams below back the cross-platform (Avalonia + CLI) editors. Read alongside
the "### Graphics System" overview in `CLAUDE.md`.

- `LZ77ToolCore.cs` (Core) — LZ77 Tool Move + Recompress tabs (WinForms `ToolLZ77Form` + Avalonia
  `ToolLZ77ViewModel`). Ambient undo via `ROM.BeginUndoScope()`; LDR-first / raw-fallback pointer search
  (event-aware path out of scope — `MissingEventAwareCoverage` always set).
- `AtomicFileSetWriterCore.cs` (Core, FILE-MUTATING, TRANSACTIONAL) — stages a related output set beside
  its destinations, backs up existing entries, commits with no-overwrite moves, and rolls back the set on
  failure. This prevents case/symlink/hard-link aliases from turning multi-artifact CLI writes into
  success-shaped overwrite corruption; used by `--convertmap1picture` tile/TSA/palette output. #1941.
- `ImageBattleScreenCore` — `EncodeTSAKeep` (Core, PURE) + `ImportBattleScreenBulk` (Core, ROM-MUTATING):
  BULK whole-screen import for the Battle Screen Layout editor (#988). `EncodeTSAKeep` keeps the TSA + applies
  the INVERSE per-cell flip; `ImportBattleScreenBulk` validate-all-before-mutate (5 strips LZ77-written +
  repointed, then palette, ambient undo, no partial commit). **Palette = SAFE single bank** (`BULK_MAX_COLORS=16`;
  >16-color REJECTED; merges into bank 0, banks 1–15 kept). #989.
- `SkillSystemsAnimeExportCore.cs` (Core, READ-ONLY) — SkillSystems skill-anime EXPORT: `SkipCode` (FE8J
  direct; FE8U skips `.dmp` + flags defender), `ExportSkillAnimation` (frames to `0xFFFF`, LZ77-decompresses
  OBJ+TSA), `BuildScriptLines`, `GetFrameImage` (#1010). Avalonia `SkillConfigAnimeExportHelper` → `.txt`+PNG/GIF
  (FE8N Ver1 stub); `SkillConfigAnimePreview` per-frame preview on the 4 SkillConfig editors. #910/#912/#1010.
- `SkillSystemsAnimeImportCore.cs` (Core, ROM-MUTATING) — SkillSystems skill-anime IMPORT (FE8J #916 / FE8U #917).
  `ParseScript` reads `D`/`S{hex}`/`{wait} {png}`; `ImportSkillAnimation` validate-all-before-mutate, palette RAW
  0x20, forces 240×160, repoints LAST under one undo scope, byte-identical restore. FE8U prepends `.dmp` template
  (`FE8USkillTemplate`). Old-region recycle (`EnumerateOldAnimeRegions` #914); ≥2-owner region never recycled (#929).
- `SkillConfigSkillSystemBulkExportCore.cs` (Core, READ-ONLY) — `ExportAll` bulk-exports the
  SkillSystems skill config to `*.SkillConfig.tsv` (`textID<TAB>animePtr` rows) + per-skill anime
  dirs; `writeAnime` delegate keeps Core GUI-free. #920.
- `SkillConfigSkillSystemBulkImportCore.cs` (Core, ROM-MUTATING, BULK-ATOMIC) — `ImportAll` re-imports every
  skill as **ONE atomic transaction**: all commit (one undo record) or byte-identical restore. Length-aware
  restore + return-value fault detection + one shared `ROM.BeginUndoScope` (per-skill `manageSnapshot:false`);
  validate-all-before-mutate; cross-slot recycle of NON-shared regions only (#929). #923/#885.
- `MapPListResolverCore.cs` (Core, READ-ONLY) — map-PLIST label resolver (`MAP Ch1`, `MAPCHANGE Ch5`,
  `ANIME1/2`, `OBJ`, `NULL`, `-EMPTY-`, `UNK`) for the MapPointer/MapChange/MapTileAnimation editors,
  via `PLists` + `ResolveLabel`. Extends (does NOT fork) `MapChangeCore.PlistType`; per-call LOCAL cache. Drives
  the anime1 filter (`MapTileAnimation1Core.BuildPlistList`/`ScanEntries`) + anime2 — **anime1 schema is the
  inverse of anime2** (`imagePointer` at `+4`, not `+0`). #952/#955/#957.
- `MapRenderCore.cs` (Core, READ-ONLY) — chapter-map + change-map overlay renderer (ports
  `ImageUtilMap.DrawMap`/`DrawChangeMap`). `RenderMapImage`/`RenderChangeMap` take an optional `obj2Offset` for
  the **FE7 obj2 dual-tileset split**: `obj_plist` high byte selects a secondary tileset whose LZ77 bytes are
  concatenated AFTER the primary OBJ bytes (WF order). FE6/8 unchanged. #961.
- `MapStyleEditorViewModel.TryImportObjImage` (Avalonia, ROM-MUTATING) — Map Style Editor OBJ import
  with the FE7 obj2 split: tile sheet split by BYTE length → primary OBJECT + obj2 PLISTs, each
  LZ77-compressed and written under ONE undo scope (a failed 2nd write rolls both back). #976.
- `EventMapChangeViewModel.ImportChangeDataFromPointer` (Avalonia, ROM-MUTATING) — Map Change Event
  pointer-import: append a COPY of `B3×B4×2` source u16 bytes to free space + repoint P8
  (append+repoint, never overwrites in place). All writes through the ambient undo overload. #961.
- `TranslateTextUtilCore.cs` (Core, READ-ONLY, NETWORK-OPTIONAL) — Text Editor Translate tab:
  `SplitEscapeSegments` (lossless literal/`@XXXX` split; `@` is a code boundary only before exactly
  4 hex digits, so `email@x` stays literal), `LoadFixedDic` (shipped glossary; only successful loads
  cached), `TranslateText` (code segments kept VERBATIM, glossary-first, injectable translator so
  tests run offline). #949/#967/#971.
- `UnitPaletteClassResolverCore.cs` (Core, READ-ONLY) — `ResolveDefaultPreviewClass(rom, slotIndex)`
  returns the first class using a unit-palette slot so the Unit Palette Editor seeds Battle Animation
  + preview (FE8 scans dedicated tables; FE6/7 read per-unit palette ids + a `GetHighClass` port).
  `FindFirstClassWithAnime` is the fallback. Pure, guards every address, never throws. #985.
- `WaitIconRenderCore.cs` (Core, READ-ONLY) — Unit Wait Icon decode + crop (#991), extracted verbatim
  from `PreviewIconHelper.LoadClassWaitIcon` (now a thin wrapper → single source of truth).
  `RenderFrame`/`RenderFullSheet`/`RenderClassWaitIcon` crop per animType (0/1/2; the #342/#667 Y=8
  offset for type 1); `GetPaletteColors` maps 0..6 self/npc/enemy/gray/four/lightrune/sepia. Reads
  palettes via the rom-aware `ImageUtilCore.GetPalette(rom, …)` overload (never the ambient ROM) (#993).
- `WaitIconImportCore.cs` (Core, ROM-MUTATING) — static PNG/BMP wait-icon sheet import (#991).
  `Import` validates dims → animType (16x48→0 / 16x96→1 / 32x96→2, else NO mutation), encodes, LZ77-
  writes + repoints `+4`, writes b2 @ `+2`, ambient undo, byte-identical fault restore (#885/#923). Class
  back-refs in `ClassFormCore` (`GetClassIdWhereWaitIconId`, `GetClassMoveIcon` move-icon id 1-BASED #993).
- `EventScriptReferenceScanner.cs` (Core, READ-ONLY) — generic event-script cross-reference scanner
  (#990; ports `EventCondForm.MakeEventScriptPointer` + FE7 tutorial table). `EnumerateEventEntries`
  yields every event entry point with verified per-cond geometry (TURN/TALK/OBJECT/ALWAYS/TUTORIAL/
  START/END, shop/chest skipped); `FindAllArgReferences(rom, argType, keepZeroId)` GATES on
  `CoreState.EventScript`/`CommentCache` wired AND `ReferenceEquals(CoreState.ROM, rom)`, disassembles
  each entry, buckets refs by id, dedups by script-start. (Text-id path's patch+ASM/MAP refs closed by #1027.)
- `BGReferenceFinder.cs` (Core, READ-ONLY) — thin `ArgType.BG` wrapper over
  `EventScriptReferenceScanner.FindAllArgReferences` with a per-ROM-instance cache (#990; Background Image
  editor References list). **Cache-poisoning guard (#992):** returns empty WITHOUT caching when scanner
  prerequisites unsatisfied, so an early/headless call can't pin a permanently-empty cache.
- `BattleAnimeRendererCore.CountAnimationPaletteBanks`/`MaxOamPaletteBank` (Core, READ-ONLY) — 32-color
  banner detector (#1033) replacing WF `GetPalette16Count`: scans all sections/frames' OAM for the max
  non-affine, non-bug (`bank<4`) 16-color bank → `max+1` (≥2 ⇒ banner); CONSERVATIVE; safe default 1.
- `ClassOPDemoFontRenderCore.cs` (Core, R/O) — Class OP Demo N1 JP-name font-glyph preview (#1032; ports WF
  `OPClassFontForm.DrawFontByID`). `RenderGlyphById(rom,id)` bounded-scans the 4-byte-pointer table at
  `op_class_font_pointer` → `RenderGlyphImage` LZ77-decompresses the 4bpp glyph → 32×32. Avalonia `ClassOPDemoView`.
- `OPClassFontImportCore.cs` (Core, ROM-MUTATING) — OP Class Font glyph PNG import (#999; wait-icon pattern).
  `Import` validates dims (%8), `EncodeDirectTiles4bpp` + LZ77-writes + repoints the D0 glyph pointer (ambient
  undo, byte-identical fault restore #885/#923). Avalonia `OPClassFontViewerView.ImportPng_Click` remaps onto palette.
- `FontGlyphRenderCore`/`FontBulk{Export,Import}Core` (Core) — main game-font glyph editor (#1165; WF `FontForm`):
  `EnumerateGlyphs`/`RenderGlyph` (2bpp-linear 16x16, fixed 4-color palette, NOT 4bpp-tiled) + `ImportGlyph`
  (find-or-append, repoint, ambient undo, byte-identical restore) + atomic `.fontall.txt` bulk. Avalonia
  `FontEditorView` glyph-grid icons; `.ttf`/`.otf` auto-gen deferred.
- `ImageWorldMapCore.ImportIconStrip`/`TryGetStripPalette` (Core, ROM-MUTATING / READ-ONLY) — World Map
  Mini/Point1/Point2/Road single-LZ77 strip imports (#1000; wait-icon pattern): validate dims (%8), encode +
  LZ77-write + repoint the ONE image pointer; palette NOT written; fault-restore. Avalonia `WorldMapImageView`
  4 handlers + FE8-only `CanImport{strip}` gates.
- `WorldMapEventResolverCore.GetEventByMapID(rom,mapid,isSelect)` (Core, READ-ONLY) — version-branched world-map-event resolver for the Avalonia Export-EA-Event tool (#1420; ports WF `ToolExportEAEventForm` dispatch): FE8 indexes stageclear/stageselect tables; FE7 = WMAP-plist byte→stageselect table (stageclear=0x0); FE6→`MapChangeCore.PlistToOffsetAddr(WORLDMAP_FE6ONLY)`. FE6/FE7 select→NOT_FOUND; full-slot guards.
- `WorldMapPathCore.ExportPathBinFromRom`/`DecodePathBin` (Core, R/O, #1458; WF `WorldMapPathEditorForm` SaveAS/Load) — `.road.bin` file I/O for the Avalonia Road (Path) editor: Save exports the RAW packed stream via `RebuildProducerCore.CalcPathDataLength`+`getBinaryData` (NOT a PackPath re-emit, so non-canonical streams round-trip byte-for-byte); Load `DecodePathBin` buffer-decodes (all-or-nothing hardening vs WF permissive `LoadPathLow`)→VM `ImportPathBin` replaces `Chips`+MarkDirty, NO ROM undo (buffer replace; Write stays the undo-tracked commit). FE8-only; `WorldMapPathEditorView` Save/Load `.road.bin` buttons via `FileDialogHelper`.
- `WorldMapPathMoveCore.cs` (Core, FE8-only, #1598; WF `WorldMapPathMoveEditorForm`) — Path MOVEMENT editor corruption fix: `MakePathList` (reuse `WorldMapPathCore`), `ResolveMovementBase` = `p32(record+8)` (the 8-byte movement sub-table, NOT the 12-byte path-record table), `LoadMovementList` (u16 ElapsedTime@+0/X@+4/Y@+6, 0xFFFFFFFF terminator NOT emitted, all-or-nothing), `WriteNode` (validate target IS a real movement node via `IsNodeInAnyMovementList` ⇒ record-base/terminator REJECTED zero-mutation, byte-identical fault restore, ambient undo). Avalonia `WorldMapPathMoveEditorViewModel`/`View` add a PathType selector (empty list + Write-disabled until a path is picked); removed the corrupting `worldmap_road_pointer` fallback + the u32 ElapsedTime read.
- `StructExportCore.FormatSTRUCT`/`FormatNMM` (Core, READ-ONLY, PURE) — Struct Dump Selector STRUCT (.h
  C-header) + NMM (No$gba memory map) export over `StructMetadata.StructDef`; Avalonia
  `DumpStructSelectDialogViewModel.MakeExportText` routes STRUCT/NMM (+CSV/TSV/EA), hex stub for unresolved. #1012.
  `FormatJSON`/`ParseJSON`/`ExportToJSON`/`ImportFromJSON` (#1937) add a JSON array-of-objects sibling to
  TSV/CSV/EA for `--export-data`/`--import-data`: public key `Index` (never internal `_Index`), one key per
  field, every value a JSON **string** in the same hex/text form as TSV. `ExportTable` keeps the complete
  `uint` row number (`0x0100` at row 256, never a byte-truncated `0x00`) for every output format. The FE6
  and FE7/8 unit metadata mirrors the canonical ViewModel layout: `MapFace`/`Affinity`/`SortOrder`/`Level`
  at `0x08`–`0x0B`, base stats at `0x0C`–`0x13`, ranks at `0x14`–`0x1B`, growths at `0x1C`–`0x22`,
  abilities at `0x28`–`0x2B`, and support pointer at `0x2C`. `ParseJSON` fully validates the
  document (array root, object rows, string-only values, no duplicate properties, strict `Index` parsing —
  no silent alias to row 0 on garbage/overflow/negative) before any ROM write. `ValidateJSONEntries`/
  `ParseAndValidateJSON`/`ImportFromJSON(path, structDef, entryCount)` (#1937 follow-up) add a second,
  struct/count-aware semantic preflight that the shape-only `ParseJSON` deliberately does not do (kept
  unchanged as a shape-only overload for existing callers): every non-`Index` property name must be a known
  `structDef.Fields` name (unknown/typo'd names rejected with row+property, a field simply absent from a row
  stays allowed for partial updates); every field value is strictly re-parsed as a complete `0x`/`$`/decimal
  token — rejecting trailing tokens, bare prefixes, negatives, and overflow — and range-checked against the
  field's `FieldType` width (Byte/Word/DWord/Pointer), with accepted values rewritten in place to a canonical
  lowercase-`0x` hexadecimal form (including accepted decimal input) so the later `U.atoi0x` call parses the
  full unsigned field range safely; no two rows may share an `Index`; and every `Index` must be in `[0, entryCount)`
  for the resolved table, rejected here instead of relying on `WriteTable`'s silent per-row skip. CLI
  `RunImportData`'s JSON branch calls `ImportFromJSON(inputPath, structDef, table.GetEntryCount(rom))` so every
  check above runs — and can throw `FormatException` — strictly before `WriteTable`/`ROM.Save`.
  `FormatCData`/`ExportToCData` + `BuildCLayout` (#1939, READ-ONLY) add an export-only GNU11 decomp-C
  sibling: one shared traversal runs in typed-only mode for TSV/CSV/EA/JSON (no unused raw-stride
  allocations) and captures typed fields + exact runtime-stride bytes once for C; the CLI never
  pre-exports C rows through the legacy path. The formatter partitions every stride byte exactly once
  into typed fields, raw gaps/trailing bytes,
  or connected-overlap unions initialized through one raw arm. It emits a packed row struct, 4-byte-
  aligned deterministic data/count symbols, a stride `_Static_assert`, compiler-visible full-width
  ordinal array designators with escaped labels, and a real GNU `[0]` symbol for
  empty/version-absent tables. `--c-symbol` is strict and never
  sanitized: file-scope-reserved names and identifiers reserved by the generated `<stdint.h>`
  prologue are rejected before output; every generated identifier is protected after includes by a
  deterministic `#ifdef`/`#undef` guard so toolchain-predefined/caller `-D` macros cannot rewrite the
  translation unit. C import/round-trip, pointer relocation, and external build orchestration are
  deliberately excluded. See `docs/febuilder-cli-as-decomp-c-backend.md`.
- `BattleAnimeRendererCore.RenderSampleBattleAnime` optional EXACT-32-byte `overridePaletteBlock` (Core, READ-ONLY)
  — live-recolor the Unit Palette editor sample preview from R/G/B spinners: Avalonia
  `ImageUnitPaletteView` packs 16 spinners (`UnitPaletteWriteCore.PackRgb555`) → `RenderClassSamplePreview(...,
  editedBlock)` ONLY when `PaletteTypeIndex==EditableBlockIndex`. #1022.
- `SongTrackChangeCore.ApplyTrackChange` (ROM-MUTATING) + `SongMidiCore.ParseSingleTrackFromDataOffset` (R/O) —
  single-track Track Change writer (#1002 Slice 1; ports WF `SongUtil.ChangeTrackAndWrite`): voice(0xBD) remap +
  VOL/PAN/TEMPO clamp + gated note-velocity; validate-before-mutate, ambient undo, byte-identical fault restore.
  Avalonia `SongTrackChangeTrackView`/`SongTrackAllChangeTrackView`. Slice 3 = `SongExchangeCore.ConvertSong`
  cross-ROM transplant (Instrument Rip/Burn, sample recycle, NO ROM growth); `SongExchangeView` + CLI `--songexchange`.
- `NameResolver.GetFaceTranslateInfo` + `ToolTranslateROMCore.AppendAIHintMessage` (Core, R/O) — Text Editor AI Hints export (#1028): `GetTranslateInfoByFaceID` (unit-table-by-face-at-+6) + `AppendAIHintMessage` (escape forms, dedup, mob fallback); Avalonia `ExportAllTexts`
- `PatchDetection.SearchAntiHuffmanPatch(rom)` (Core, R/O) — Text Editor bad-char popup (#1028 Slice D): 6 un-Huffman sigs; `PatchDetectionService.DetectAntiHuffman`. `TextViewerViewModel.WriteText` = encode-fail→`TextBadCharPopupView`→ABORT via `EncodeAbortedException`.
- `ExportFilterCore.BuildFilteredTextIds(rom,idx)` (Core, R/O) — Text Editor Export Filter (#1028): `InitExportFilter` 11 cats (0=All→null); BattleTalk/Haiku event-ptr-when-0; Skill via `SkillSystemTextScanner`; EventCond via `EventScriptReferenceScanner.CollectEventCondTextIds`.
- `MakeVarsIDArrayCore.BuildAllUsedRefs/BuildFreeAreaUsedSet` + `TextFreeAreaCore.FindUnreferencedTextIds` (Core, R/O) — DEFINITIVE Text Editor free-area + cross-ref (#1027; faithful `U.MakeVarsIDArray`): typed TEXTID∪SONG union over Unit/Class/Item/EventCond + collectors, reusing ExportFilterCore; `AsmMapTextSymbolReader` + `PatchTextRefScannerCore` + `ITextIDCache`. PREREQ-GUARD when EventScript/CommentCache unwired or ROM≠active. Avalonia `FindUnreferencedTexts`/`FindCrossReferences`.
- `ToolAnimationCreatorViewViewModel.InitFromMagicRom`/`InitFromSkillRom` (Avalonia, R/O) — Animation-Creator jump seeds: magic #996 (FEditor/CSA 0x86 via `MagicEffectExportCore`) + skill #1115 (`ExportSkillAnimation`; `CountSkillFrames`; `SkillConfigAnimeJumpHelper` 4 variants, Ver1 render-only). MapAction write-back only.
- `PatchMacroAddressResolverCore.cs` (Core, R/O) — `$GREP`/`$XGREP`/`$FGREP`/`$P32`/`$TEXTID` resolver; adds `GrepPatternMatchEnd/Begin` to `U.cs`; wires `PatchTextRefScannerCore` so grep-resolved TEXT/SONG/EVENT refs reach the free-area union. #1027.
- `DecompProject.cs` (Core, READ-ONLY, never-throws) — decomp open mode (#1129): `Detect`+`ResolveBuiltRom`; `IsDecompMode`; CLI `--project`. #1130 `DecompSymbolResolver` (`--resolve-addr`); #1131 `DecompDiffMigrationCore` (`--migrate-diff`); #1133 `DecompAssetExportCore`+`IndexedPngWriter` (`--export-asset`; #1148 `ImportMap`/`--import-asset`/`--roundtrip-asset`: `.mar`→blob). #1132/#1141/#1148 `DecompSourceWriterCore` C+JSON (items/units/classes/map_settings); chapter scalars source-backed; pointers+raw map ASSETs ROM-only/manual (`DecompMapAssetGuard`). #1150 `DecompRoundTripAuditCore` (`--decomp-audit`)+`NmmSchemaBridgeCore` (`--nmm-to-manifest`/`--manifest-to-nmm`)+`IndexedPngReader`/`DecompAssetValidatorCore` (`--validate-asset`)
- `ProjectionFileSystemSafety.cs` (Core, READ-ONLY, #1935) — shared fail-closed regular-file gate used by `BuildfileExportCore`, `RebuildMakeCore`, and buildfile CLI ROM ingestion. Unix first uses .NET 10's normalized `SystemNative_LStat`, then native `open(O_NOFOLLOW|O_NONBLOCK|O_CLOEXEC)` plus `SystemNative_FStat`; only the same `(dev,inode)` `S_IFREG` entry is accepted and the returned `FileStream` owns that exact descriptor. Windows uses `CreateFileW(FILE_FLAG_OPEN_REPARSE_POINT)` and validates the exact handle as a non-reparse disk file. Exact opened-handle identity uses volume + 128-bit `FileIdInfo`, falling back to the legacy 64-bit file index only when that capability is unavailable (for example FAT/exFAT). Metadata/native-entry failure, replacement, symlink, FIFO, socket, or device refuses before any unbounded read.
- `ProjectionTreeSnapshot.cs` (Core, READ-ONLY, #1935) — bounded immutable capture of advisory projection output without re-resolving descendant pathnames after the root opens. Unix enumerates `fdopendir(dup(fd))` through .NET 10's `SystemNative_ReadDir` ABI and opens every child with `openat(O_NOFOLLOW|O_NONBLOCK)`; Windows enumerates a held directory with `NtQueryDirectoryFile` and opens children through `NtCreateFile(OBJECT_ATTRIBUTES.RootDirectory)`. Exact handles must identify plain directories/regular files; links, devices, FIFOs, sockets, invalid UTF-8 names, depth over 256, oversized path metadata (4,096 characters per relative path / 8 Mi characters total), more than 32,768 entries, or more than 256 MiB fail the advisory projection closed. Empty directories are explicit snapshot entries.
- `BuildfileExportCore.cs` (Core, FILESYSTEM-MUTATING via staged publish, planner is PURE, #1935; CLI `--export-buildfile`) — deterministic clean→modded **buildfile recipe** exporter. `Plan(cleanRom,targetRom,options)` validates same-supported-version identity + size bounds (reject shorter-than-clean / >32 MiB), picks the deterministic extension fill (most frequent byte in `[clean.Length,target.Length)`, lowest byte on ties), then derives the **authoritative** payload ranges DIRECTLY from a bounded fill-aware diff — `RomDiffCore.CompareWithFillBounded(baseline,target,fill,maxRanges)` (maxGap:0 preserved: unchanged bytes are never folded into a payload) — never from `DecompDiffMigrationCore.AnalyzeWithFill`'s coalesced report list, so the authoritative range set can no longer drift from whatever `AnalyzeWithFill` happened to report. `BuildfileExportOptions.MaxPayloadRanges` (default 16384) bounds worst-case fragmentation (e.g. a 32 MiB alternating-byte diff): the bounded overload throws a stable, path-free `RomDiffCore.DiffRangeLimitExceededException` the instant the next range would exceed the limit — BEFORE any payload/manifest materialization — while the existing unbounded 3-arg `RomDiffCore.CompareWithFill` overload is untouched for other callers. Each authoritative range is then classified **independently and advisorily** via the new `DecompDiffMigrationCore.ClassifyRangeSafe` seam: a faulting/null/exception-throwing classifier (including via the internal `BuildfileExportOptions.ClassifierOverrideForTest` injection seam, default null/no production behavior change) degrades ONLY that range's own category/confidence/suggestion to a stable `unknown`/`low`/manual-review fallback — it can never omit, reorder, resize, or otherwise mutate the authoritative range itself, and never aborts export; `DecompDiffMigrationCore.AnalyzeInternal` is likewise hardened to call `ClassifyRangeSafe` per coalesced range instead of returning a truncated partial report after one classifier fault. Manifest `gbaAddress` values always use the bounded ROM-offset mapping `0x08000000 + offset`, including offsets 0 and 1. Classifier symbol lookup explicitly maps those two sentinel offsets while retaining `U.toPointer`'s established behavior for every other value, including out-of-window `--migrate-diff` input. `Export(...)` derives a bounded fixed-ASCII stage/scratch prefix from a stable hash of the output basename, atomically reserves each generated sibling with native create-new semantics, and retries collisions without touching the pre-existing tree (Windows uses an internal extended-length drive/UNC spelling so raw `CreateDirectoryW` retains managed >`MAX_PATH` support), then writes raw `data/<index>_<offset>_<len>.bin` payloads + derived `main.event` (PUSH/`ORG`/`FILL Amount Value`/`#incbin`/POP; ColorzCore fills byte-wise, the non-skippable structural test pins its two-operand parser contract, and a gated real-ColorzCore round trip proves the declared fill and payloads reconstruct exactly) + advisory installed-patch inventory (uses the bounded `PatchMetadataCore.TryEnumeratePatchesBounded`/`TryParsePatchFileStrictBounded`/`TryParsePatchParamsBounded` path, sorted by definition-relative path, installed/unknown/unavailable + confidence/reason, never promotes unknown; production discovery lazily scans `Directory.EnumerateFiles` and stops at the shared cap before parsing another file, so a missing patch ROOT at initial discovery is successful-empty while a definition file that was already discovered but is missing/faulting when the bounded metadata pass opens it — and any inaccessible directory — is a true `unavailable` filesystem failure (never success-empty, never a resource-budget reason), and a discovery/metadata/param resource-limit breach OR bounded-read content drift degrades the whole inventory without truncation; enumeration/raw-parameter/relative-path failures are surfaced as STABLE, path-free literal manifest reasons, including the distinct changed-source reason `advisory patch source changed during bounded read`, never the raw exception message or the absolute patch base directory/patch file path; legacy Patch Manager/CLI `EnumeratePatches` remains tolerant per file, logging an unreadable definition while retaining other entries; the CLI itself still resolves the version dir by existence ONLY, no enumeration, so patch faults can never abort export) + optional `--with-source` `RebuildProducerCore.MakeWithProducer` projection (`RebuildMakeCore` uses exception-propagating file writes; before success, `ValidateProjectionOutput` checks every sidecar's ancestor chain before opening it, then requires a readable regular manifest with both required headers and every referenced sidecar to be a readable regular file beneath scratch with no symlink/junction directory in its ancestor chain; scratch is a unique SIBLING OUTSIDE the publish stage on the same volume, and its root plus every descendant directory must remain plain non-reparse directories before sanitation and immediately around the move to `source/`; a replaced symlink/junction root is refused without touching its external target and is deleted non-recursively; successful projection moves only after LF-normalization + exporter-owned scratch-path stripping via the shared `SanitizeScratchPath` helper, applied uniformly to the projection outcome's success/refused/error/exception `Reason`, the `publish failed:` message, and the final cleanup-failure message — refusal/error ⇒ warning + direct-attribute delete-and-VERIFY-gone, abort export if the scratch can't be removed or absence cannot be inspected; never publishes `.psrc-*`/partial `source/`; non-composable; `SanitizeScratchPath` strips the exporter-owned scratch absolute path in its native/forward-slash/JSON-escaped spellings using case-insensitive matching on Windows/macOS and case-sensitive matching elsewhere, not arbitrary paths), RE-READS/hashes every payload, then **after** `RunProjection` has finalized manifest projection status/warnings — and after `TryPrepareManifestForByteCap` (below) has run so any byte-cap-driven advisory degradation is already reflected in `patches.status`/`patches.reason`/cleared `patches.installed` before either file is written — writes README (so a projection refusal/error warning is guaranteed to appear consistently in README, manifest, and CLI-facing warnings; byte-cap degradation itself is represented ONLY by those three `patches.*` fields — it never adds or alters a `BuildfileManifest.Warnings` entry, the generated README's warning section, or a result/CLI-facing warning, unless a separate, independent condition does so) and finally `buildfile.json` LAST, then publishes with one `Directory.Move`. Producer/consumer manifest-size mismatch closure (#1936/#1935 follow-up): before BOTH that initial write and any later rewrite (see below), `TryPrepareManifestForByteCap` measures the exact UTF-8 byte count of `SerializeManifest(manifest)` (the SAME LF-terminated text that is actually written) against `BuildfileExportOptions.MaxManifestBytes` — a `public const` alias of the single source of truth `BuildfileFormat.MaxManifestBytes` (16 MiB), the EXACT SAME constant `BuildfileBuildOptions.MaxManifestBytes` aliases, so the exporter never publishes a manifest that its own independent consumer would refuse SOLELY for exceeding that shared byte cap — every other structural/identity/payload/filesystem validation the consumer performs, and any post-publication mutation of the published project, remain independently enforced regardless of the manifest's origin. Within budget, nothing changes. Over budget with a non-empty advisory `Patches.Installed`, the WHOLE inventory (never a partial/truncated list, and never any authoritative range/payload/identity/extension/projection field) degrades to `unavailable` with a new, distinct, stable, path-free `ManifestByteBudgetExceededReason` (kept separate from the unrelated resource-budget `AdvisoryResourceBudgetExceededReason`), and the manifest is re-measured. If it is STILL over budget afterward — or there was no installed inventory left to degrade — `Export` throws before either file is written, so the existing outer catch's stage/scratch cleanup runs and no over-cap manifest is ever published; no new cleanup logic was added for this path. An internal, non-widening `BuildfileExportOptions.MaxManifestBytesForTest`/`ResolveManifestByteCap` seam (validated to `1..MaxManifestBytes`, never exposed via CLI/config/env) lets tests prove pass-at-cap/fail-at-cap+1/degrade/abort behavior without materializing a real 16 MiB fixture; production always resolves to the immutable constant. Neither ROM is mutated; failure never publishes the destination and verifies stage/scratch cleanup; blocked cleanup is named explicitly with its residual path. Deterministic output: JSON property order/indentation, LF endings, forward-slash relative paths, no absolute paths. `buildfile.json` + `data/` are the SOLE build authority (applied/verified by #1936; emulator/playtest is #1932); non-canonical same-version clean = explicit warning (clean SHA-256 is the reproducibility identity). Path safety via `BuildfilePathSafety` (normalize = `GetFullPath`+`TrimEndingDirectorySeparator` so `project/`≡`project`; `ContainsParentTraversal` raw-segment `..` detector; public realpath `ResolvePhysicalPath` itself rejects raw `..` before normalization, then follows symlink/junction targets INCLUDING ancestor links; Unix preserves link-target `.`/`..` components until preceding target links have been physically resolved, while Windows mirrors Win32's lexical link-target normalization before the physical walk; relative-target/loop/permission/missing-before-traversal faults fail closed and are never broad-caught; `PathsEqual`/`SamePhysicalFile`; single-entry `IsReparsePoint`): CLI also rejects raw `..` in `--rom`/`--clean` with command-specific diagnostics, resolves each input to its physical realpath ONCE, preflights both resolved file lengths BEFORE either ROM load (reject target shorter than clean or >32 MiB without allocating it), and uses those exact paths for BOTH same-physical-file identity and loading (catches a junction/symlink parent aliasing two lexically-different inputs; two distinct ROMs sharing a benign symlinked ancestor like macOS `/var`→`/private/var` are ACCEPTED), rejects a reparse-point output parent (single-entry — atomic publish needs only a shared immediate parent), and an unknown command-specific flag allowlist — all BEFORE load/write; a final symlink to a DISTINCT file is allowed; macOS case comparison is conservatively case-insensitive; after exact opening, native handle identity rejects same-file aliases, including hard links, on Unix and Windows.
  Publication implementation note (superseding the earlier `Directory.Move` shorthand):
  the stage is committed with an atomic **no-replace** rename (`MoveFileExW` without
  replace flags, Linux `renameat2(RENAME_NOREPLACE)`, Darwin `renamex_np(RENAME_EXCL)`).
  A destination created after preflight, including an empty directory, is preserved and
  the export fails; unsupported native entry points fail closed.
  Advisory budget / fail-closed cleanup implementation note (#1965 PR feedback remediation):
  `BuildPatchInventory` is now a BOUNDED producer sharing ONE running total
  (`BuildfileFormat.MaxAdvisoryItems` = 16,384) with the manifest's `warnings` — patch-file
  discovery uses the SAME bounded lazy `foreach` for both production `Directory.EnumerateFiles`
  and injected arrays/custom iterators — there is no separate eager length guard — and each
  record/its raw params are counted against the SAME shared total, consumed via
  `PatchMetadataCore.TryEnumeratePatchesBounded`/`TryParsePatchParamsBounded`, BEFORE being
  added to `Installed`. `TryEnumeratePatchesBounded`/`TryParsePatchFileStrictBounded`/
  `TryParsePatchParamsBounded` now propagate an explicit typed failure kind
  (`None`/`ResourceLimit`/`ContentChanged`/`FileSystem`) rather than a boolean: genuine
  discovery/metadata/params resource bounds map to the stable
  `AdvisoryResourceBudgetExceededReason`, bounded-read length drift/growth/truncation maps to the
  distinct stable path-free changed-source reason `advisory patch source changed during bounded read`,
  and real filesystem/access faults keep the separate generic, path-free permission reason.
  Follow-up byte-bound hardening (further #1965 PR
  feedback remediation): per-file metadata scanning (`PatchMetadataCore.TryParsePatchFileStrictBounded`)
  and raw-parameter capture (`TryParsePatchParamsBounded`) no longer use lazy `File.ReadLines` — a
  single pathological line was still materialized in full BEFORE any line-count guard ran, an
  independent byte-level OOM vector. Both now go through one shared helper,
  `PatchMetadataCore.TryReadBoundedFileLines`, which opens the file through the SAME shared
  fail-closed, no-follow, exact-regular-file primitive as the rest of the exporter,
  `ProjectionFileSystemSafety.OpenRegularFileForRead` (#1965/#1936 correction — the bounded
  reader previously opened `PATCH_*.txt` with a plain `FileStream` constructor, which
  transparently follows a final-entry symlink/reparse point and could serialize an external
  target file's content into advisory `patches.installed[].params`); a final entry that is a
  symlink, reparse point, or any non-regular-file type is refused before a single byte is read,
  and Browser hosts fail closed with `PlatformNotSupportedException` rather than an unsafe
  fallback path. This final-entry guarantee does not extend to ancestor directories of the patch
  library (out of scope by design; see `ProjectionFileSystemSafety.cs`'s own ancestor-link
  handling for the projection/ROM paths that DO cover ancestors). Once opened, the helper
  rejects on `FileStream.Length` alone before any read (defeating a
  sparse/huge-length file), then reads at most `cap+1` bytes on that SAME handle so a file that
  grows after the `Length` check is still caught, and only THEN decodes the accepted bytes with a
  default-constructed `StreamReader` (BOM auto-detection + non-strict UTF-8 replacement fallback),
  matching `File.ReadLines`/`File.ReadAllLines` decode semantics byte-for-byte on any within-budget
  file. Any breach — `Length` over cap, or cap+1 bytes read — returns no lines at all; a partial
  line is never decoded or returned. Length-drift correction (#1965 follow-up): bytes reaching
  decode must equal that SAME captured `Length` EXACTLY, not merely stay at-or-under `cap`/`cap+1`.
  After every positive read, `total`/`bytesRead` are updated FIRST and then compared against BOTH
  `maxBytes` AND the captured `length` — a breach of either rejects before the surplus chunk is
  ever written into the accepting buffer, so growth past this handle's own observed `Length` is
  caught even while the running total still fits comfortably under `maxBytes` (a prior version
  only compared against `maxBytes` here). Symmetrically, once genuine EOF is reached (a read
  returning 0 — the shared no-follow opener guarantees a regular, synchronous `FileStream`, so a
  short but POSITIVE read is never itself EOF and the loop simply requests more), a running total
  LESS than the captured `length` (the file shrank/was truncated after `Length` was read) is
  rejected the same way, before `ms.Position`/`StreamReader` ever run. Bytes genuinely consumed
  before either rejection remain visible via the incrementally-updated `bytesRead` below. This is
  a length-drift DETECTOR, not an immutable-snapshot guarantee: an in-place mutation that leaves
  the file's length unchanged, or bytes appended strictly after the final observed EOF, both
  remain outside what this check can detect. The helper is CHUNKED, not single-buffer: bytes are read
  through a small, fixed, pooled (`ArrayPool<byte>`) `ReadChunkBytes` (64 KiB) request buffer —
  never one allocated to the full per-file cap — with each individual `Read` clamped to that chunk
  size (or less, near the remaining budget); the accepting `MemoryStream` starts sized only from
  the already-validated (≤ cap) `Length` as a hint (or a small default), growing solely as bytes
  actually arrive. This avoids the cumulative large-object-heap churn a naive "allocate `cap+1`
  bytes per file" design would incur across a real patch library (thousands of files, most far
  smaller than the cap). `bytesRead` is updated INCREMENTALLY after every individual successful
  chunk read (not just once at the end), so if a later read on the same handle throws a genuine
  I/O fault, the bytes genuinely consumed before that fault remain visible to the caller through
  this `out` parameter — callers (including `TryAppendRawParamsBounded`) must never reset it to
  zero in a catch block, or genuinely-read bytes would evade an aggregate byte budget. Decoded
  lines are appended ONE AT A TIME during `StreamReader.ReadLine()`, checked against an explicit
  `maxLines` argument BEFORE each line is added — never after decoding the complete file into an
  unbounded list first, closing a companion OOM vector where a within-byte-budget file consisting
  of millions of tiny/blank/comment lines (none of which ever parse as a KEY=VALUE entry, so the
  parsed-entry `maxEntries` bound never trips) could otherwise force an unbounded raw line list.
  The metadata pass reuses its existing `maxLines` bound; the raw-parameter pass uses a new
  immutable alias, `PatchMetadataCore.MaxRawParamLines` (= `BuildfileFormat.MaxAdvisoryItems`).
  On either a byte-cap or raw-line-cap breach the helper clears the line list to `null` — no
  partial result is ever produced. Each `PATCH_*.txt` is capped at `MaxPatchDefinitionBytes`
  (16 MiB); the metadata-discovery pass additionally tracks a running total against
  `MaxMetadataAggregateBytes` (a separate 64 MiB aggregate, threaded as an explicit
  `maxAggregateBytes` parameter through `TryEnumeratePatchesBounded` — always
  `PatchMetadataCore.MaxMetadataAggregateBytes` in production, never a mutable override), and the
  raw-parameter-capture pass independently tracks its own running total against
  `BuildfileExportOptions.MaxPatchParamsAggregateBytes` (also a separate 64 MiB aggregate,
  128 MiB worst case combined with the metadata pass). Production `BuildPatchInventory` is a
  thin wrapper that unconditionally binds this immutable constant into the ordinary parameterized
  internal helper `BuildPatchInventoryBounded(..., long maxParamsAggregateBytes)` — the parameter
  is reachable ONLY by a test calling `BuildPatchInventoryBounded` directly with a small
  deterministic value, never through any options field, so there is no mutable/nullable
  production-honored cap-override hook. Both aggregate caps and the per-file cap are `public
  const` — there is no code path that lets a caller widen them at runtime. `TryParsePatchParamsBounded`
  also drops its former `File.Exists` precheck (a stale-existence race window); only a
  `FileNotFoundException`/`DirectoryNotFoundException` raised while opening now maps to a
  successful-empty result (matching the old `File.Exists == false` contract) — since the
  bounded reader now opens through `ProjectionFileSystemSafety.OpenRegularFileForRead`, these
  two typed exceptions originate from that primitive's own native-error classification
  (Windows `ERROR_FILE_NOT_FOUND`/`ERROR_PATH_NOT_FOUND`; Unix `ENOENT`/`ENOTDIR` from either
  the initial `lstat` or the later no-follow `open()` race) rather than a separate existence
  check, so a genuinely missing final file or missing parent directory still degrades to
  success-empty while a rejected symlink, reparse point, or other non-regular-file type is a
  distinct, non-missing `IOException` that propagates — while
  `UnauthorizedAccessException`/`IOException`/`SecurityException`/etc. propagate to
  `TryAppendRawParamsBounded`'s caller for the existing generic path-free degradation — no broad
  catch converts an access fault into an empty/missing result, and the catch there deliberately
  does NOT reset the incrementally-accumulated `bytesRead` to zero, so bytes genuinely read before
  the fault still count against the caller's aggregate params budget. A pathological patch file can
  therefore never be fully materialized into memory — on raw bytes OR on decoded lines — just to
  extract its NAME/TYPE/PATCHED_IF metadata or its raw parameters; exceeding either bound returns no
  partial patch record and degrades the whole inventory. The legacy tolerant
  `ParsePatchFile`/`ParsePatchFileStrict`/`ParsePatchParams` paths used by `TryEnumeratePatches` and
  other non-bounded callers are unaffected and still use the original eager/lazy `File.ReadAllLines`/
  `File.ReadLines` APIs. Install-marker CLASSIFICATION carve-out (#1936): decode is byte-identical
  to legacy, but the bounded metadata pass deliberately classifies a file-backed `$FGREP` install
  marker (one `PatchMacroAddressResolverCore.Resolve` would resolve by `Path.Combine`/`File.Exists`/
  `File.ReadAllBytes` on the external file it names) as `Unknown` BEFORE the resolver is ever
  invoked — an `allowFileBackedConditions` flag threaded through the shared line parser/`CheckPatchInstalled`
  helper (legacy/public overloads pass `true`; `TryParsePatchFileStrictBounded` passes `false`),
  with `$GREP`/`$XGREP`, pointer/text macros, and fixed/bare-hex addresses all still resolved
  normally; `PatchMacroAddressResolverCore` is NOT globally hardened (256 shipped `$FGREP`
  definitions and legitimate `..` traversal keep the legacy behavior in every public/unbounded Patch
  Manager/CLI/scanner/rebuild path). A NON-PRODUCTION internal `TryParsePatchFileStrictBounded`
  overload takes an injected `MacroAddressResolver` delegate (production always binds
  `PatchMacroAddressResolverCore.Resolve`; the delegate is never reachable through options/public
  API/mutable state and cannot widen any cap) purely so a test can PROVE the resolver is never
  invoked for a file-backed `$FGREP` marker and IS invoked for a `$GREP` marker (non-vacuous). Any breach — discovery, metadata, per-record, or per-param, byte or
  aggregate-byte or item-count — degrades the ENTIRE inventory to `unavailable` with the stable
  `AdvisoryResourceBudgetExceededReason` (never a partial/truncated list). `EnforceAdvisoryBudgetAfterLateWarnings` re-checks the combined
  total after `RunProjection` and after `MarkProjectionMaterializationError` may have
  appended a warning AFTER the patch budget was already reserved, degrading patches (never
  warnings) if the late total no longer fits. Immediately before the resulting
  `RewriteProjectionMetadata` call re-serializes and re-publishes that adjusted manifest,
  `TryPrepareManifestForByteCap` (see the `BuildfileExportCore.cs` entry above) re-measures the
  manifest's exact UTF-8 byte count a SECOND time — a materialization-error warning added at this
  late point can itself be what pushes an already-at-the-line manifest over the shared 16 MiB
  cap — degrading the advisory inventory again (or aborting before this late write, with the same
  existing stage/scratch cleanup) if needed, so a late rewrite can never re-introduce an over-cap
  manifest that the earlier, initial-write check had already cleared. Separately, `VerifyPathAbsent`/`DeleteAndVerifyGone`
  now classify a path's existence through the shared internal tri-state
  `ProbePathAttributes` helper instead of `File.Exists`: only `FileNotFoundException`/
  `DirectoryNotFoundException` mean absent; ANY successfully returned attributes (a regular
  file, a directory, or a reparse point) mean present; any other expected filesystem/access
  fault is `Unknown` and fails closed with the exact path — `File.Exists` could otherwise
  silently report "gone" for a directory/reparse-point replacement or an inspection failure.
  Projection implementation note (superseding the legacy move/in-place-sanitize description in the
  long entry above): production `--with-source` has no public arbitrary-runner hook. The built-in
  synchronous projector finishes in a private scratch while no publish stage exists. Manifest
  parsing uses the 16 MiB projection-text ceiling, caps sidecar directives at the 32,768-entry
  ceiling, bounds line count/length, and opens referenced sidecars without pre-reading their
  bodies. The exporter captures that tree handle-relatively into bounded managed memory, rejects
  any individual text file above 16 MiB before allocation, and sanitizes strict UTF-8 text only
  (a UTF-8 BOM is accepted and removed; UTF-16/32 BOMs are rejected),
  deletes/verifies scratch absence, and only then reserves the private stage. A fresh `source/` is
  materialized from the immutable snapshot immediately before the no-replace publish; no
  runner-owned inode or hard link enters the stage. The internal source-swap fault seam validates
  its candidate, then discards every candidate inode and rematerializes from the snapshot, so even
  a regular hard-link replacement cannot survive into publication. Late unsafe source replacement
  downgrades the advisory projection and rewrites README/manifest consistently; cleanup failure aborts.
  CLI ROM ingestion likewise supersedes the earlier pathname-loading shorthand: both resolved
  inputs are opened as exact regular-file handles, the opened identities are compared again
  (Windows volume/128-bit `FileIdInfo`, with capability-only legacy fallback; Unix device/inode), their lengths are bounded before allocation, and
  their bytes are read exactly once through those handles before `ROM.LoadFromBytes`.
  Browser builds fail closed before pathname access because they cannot provide those exact-open
  and opened-file identity guarantees.
  Windows path hardening: `NormalizeFullPath` rejects the entire Win32 device-namespace class
  (`\\?\`, `\\.\`, `\??\`, extended-drive/extended-UNC, and slash variants after `GetFullPath`)
  before component inspection; `ResolvePhysicalPath` and link-target normalization share that
  boundary, and raw traversal detection recognizes drive-relative `C:..\...` spellings before
  normalization. `SameResolvedPhysicalFile` adds Windows volume-serial + 128-bit file-ID comparison
  with a FAT/FAT32/exFAT-compatible 64-bit handle-identity fallback, closing hard-link,
  mounted-drive, and local-drive/UNC aliases after the one realpath walk; its raw `CreateFileW`
  handles use internal extended-length spellings so standard long drive/UNC inputs remain supported.
  The internal projection override and source-swap callbacks exist only for deterministic tests;
  their contract is synchronous. A same-identity process can always mutate user-owned loose files
  after publication, so downstream consumers must revalidate files they consume.
- `BuildfileFormat.cs` (Core, PURE, #1936) — canonical schema-v1 spellings shared by the exporter and the consumer so the EMIT and VALIDATE sides can never diverge: lowercase `Sha256Hex`, `Crc32`/`Crc32Hex` (the exporter's `U.CRC32` table), `0x`-uppercase `Hex32`/`Hex8`, `GbaAddress(offset)` = `0x08000000 + offset`, canonical `PayloadName`/`PayloadPath` = `data/{index:D4}_{offset:X6}_{length}.bin`, and canonical-spelling validators `IsCanonicalHex32`/`IsCanonicalHex8`/`IsCanonicalSha256`. `BuildfileExportCore` now delegates its `PayloadName`/`Hex32`/`Hex8`/`Sha256Hex` here (identical output). #1965 PR feedback remediation additionally adds the single source-of-truth shared advisory-item resource-safety budget: `MaxAdvisoryItems = 16384` and the overflow-safe `TryConsumeAdvisoryItems(ref int total, int additional)` (checked `long` arithmetic; leaves `total` unchanged on a would-be breach). `BuildfileExportOptions.MaxAdvisoryItems`/`BuildfileBuildOptions.MaxAdvisoryItems` are `public const` aliases of this same constant (same assembly, so a public const may reference the internal one) — deliberately NOT a mutable/test-only override, so exporter and consumer can never drift apart on the cap. #1936 manifest-size closure adds the analogous single source-of-truth `internal const int MaxManifestBytes = 16 * 1024 * 1024;`; `BuildfileBuildOptions.MaxManifestBytes` (previously its own independent `16 * 1024 * 1024` literal) and the new `BuildfileExportOptions.MaxManifestBytes` are now both `public const` aliases of this ONE constant, so the exporter's own manifest-byte-cap enforcement (see the `BuildfileExportCore.cs` entry above) and the consumer's pre-existing `buildfile.json` size bound can never independently drift apart the way they previously could (only the consumer had a cap; the exporter had none).
- `BuildfileBuildCore.cs` (Core, READ-ONLY reconstruction + FILESYSTEM-MUTATING publish-only, #1936; CLI `--build-buildfile`/`--buildfile-roundtrip`) — the strict, INDEPENDENT schema-v1 CONSUMER. `Build(cleanRom, projectDir, options)` reconstructs the target ROM SOLELY from `buildfile.json` + `data/`: it never executes/parses `main.event`, invokes ColorzCore, consumes `source/`, or trusts the advisory patch inventory/projection/warnings (none can influence an output byte), and never mutates the ROM or the project. `buildfile.json` is opened via the shared `ProjectionFileSystemSafety.OpenRegularFileForRead` (exact no-follow regular file), bounded to `BuildfileBuildOptions.MaxManifestBytes` (16 MiB, now the shared `BuildfileFormat.MaxManifestBytes` alias described above rather than a private literal), decoded as strict UTF-8 (optional UTF-8 BOM; UTF-16/32 BOMs rejected), scanned for duplicate JSON property names with a `Utf8JsonReader` pass, then `JsonDocument`-validated (Disallow comments/trailing commas, MaxDepth 64) with EXPLICIT presence/type/value checks (no reliance on DTO defaults) for `schemaVersion == 1`, exact clean size/CRC32/SHA-256/canonical-flag + version equal to the clean ROM, `dataDirectory == "data"`, target ≥ clean and ≤ 32 MiB, extension present exactly when the target extends clean (`start == clean`, `length == target - clean`, canonical `0xNN` fill), ≤ 16,384 ranges with contiguous zero-based indices/exact `totalRanges`/strictly-ordered non-overlapping positive ranges/checked in-bounds offset arithmetic/canonical `gbaAddress`/`changedBytes == length`/unique canonical `data/{index:D4}_{offset:X6}_{length}.bin` payload paths/canonical `payloadSha256`/exact `totalChangedBytes`. `data/` is captured through the shared `ProjectionTreeSnapshotReader.Capture` with `maxEntries = max(1, totalRanges)` / `maxBytes = max(1, totalChangedBytes)`, snapshot relative names are mapped back under the `data/` prefix, and subdirectories, symlinks/reparse points/devices, missing/extra/case-colliding payloads, and any length/SHA-256 mismatch fail closed; hard-linked regular files are accepted by design (the reader only copies bounded bytes from held no-follow handles into an immutable snapshot — it never rewrites a project inode). Reconstruction copies clean bytes + the declared fill + validated payloads in manifest order (payloads override the fill and may span the clean/extension boundary), then recomputes the target CRC32/SHA-256. STRUCTURAL/clean/payload faults ⇒ `Success == false`; a declared-TARGET-identity mismatch is a DISTINCT `Success == true` + `TargetIdentityMatches == false` (bytes still returned) so callers classify reproducibility drift separately. Publication helpers: `PublishBytesNoReplace(data,dest)` writes an exclusively-reserved same-parent staging file (`FileMode.CreateNew`), durably flushes (`Flush(true)`), fully disposes the handle, then commits with the shared `BuildfileExportCore` native no-replace rename (Windows `MoveFileExW` w/o replace, Linux `renameat2(RENAME_NOREPLACE)`, macOS `renamex_np(RENAME_EXCL)`); a race-created destination is never overwritten and the staging file is removed+verified on any failure. `ReserveScratchDirectory`/`DeleteTreeAndVerifyGone` wrap the exporter's atomic create-new + delete-and-verify primitives for the round-trip scratch. CLI: `--build-buildfile` refuses to publish on structural fault OR declared-target mismatch (exit 1); `--buildfile-roundtrip` exports (source projection OFF) into a private atomically-reserved scratch, passes an absent child project path to the exporter, reconstructs, verifies scratch cleanup, and compares the rebuilt bytes to the exact already-opened `--rom` bytes (the SOLE oracle) via a pure first-difference comparer — byte OR declared-target drift ⇒ exit 2, exact ⇒ 0, export/reconstruction/cleanup error ⇒ 1. The internal `BuildfileRoundTripOperations` seam (CLI) defaults to the production exporter/reconstructor and is injected by `FEBuilderGBA.Tests` to map deterministic byte-drift AND declared-target-only drift through the actual handler overload to exit 2 — no hidden command flag or environment bypass. `BuildfileBuildOptions.MaxManifestBytesForTest` is an internal-only cap seam.
  Canonicality hardening additionally rejects adjacent range splitting and any payload byte equal to its clean/fill baseline; the project root is physically resolved once, staging names use the exporter's bounded hash prefix, and injected tests pin destination-race, staging-cleanup-failure, and round-trip scratch-cleanup-failure behavior.
  Advisory budget / fail-closed cleanup implementation note (#1965 PR feedback remediation):
  `PopulateAndValidateAdvisoryStructure` tracks ONE running total across `patches.installed`,
  every nested `params` array, and `warnings` — sharing the SAME `BuildfileFormat.MaxAdvisoryItems`
  budget the exporter reserves against. Each array's `JsonElement.GetArrayLength()` is consumed
  from that shared total BEFORE the array is enumerated/materialized into POCOs/lists; exceeding
  the cap is a hard structural `BuildfileValidationException` naming the combined total/limit —
  distinct from the exporter, which DEGRADES an oversized patch library instead of failing. There
  is deliberately no mutable/test-only override of the cap. Separately, `DeleteFileAndVerifyGone`
  (staging cleanup) and `WriteStagingFile`'s `FileMode.CreateNew` collision classification no
  longer use `File.Exists`: both now delete/probe then confirm through the shared
  `BuildfileExportCore.ProbePathAttributes`/`VerifyPathAbsent` tri-state contract — a present path
  (regular file, directory, or reparse point) is a genuine collision/cleanup-failure, an absent
  path is a genuine "gone"/not-a-collision, and an inspection fault fails closed with the exact
  path rather than being misread as "gone" by a bare `File.Exists` check.
- `FeInfoCodeMap.cs` + `DisassemblerCore.LoadSymbolMap` merge (Core, READ-ONLY, never-throws) — fe-info `json/{fe6,fe8}/code.json` function-name enrichment for the Avalonia/Core disassembler (#1824 Slice 1). `Parse` accepts the fe-info JSON array, resolves bare or region-keyed hex-string `addr` values with `U.atoh` (GBA pointer keys, no pointer/plain conversion), and emits `AsmMapSt.Name` plus approximate `RET=type, rN=type` signatures. `ResolveCodeJsonPath` maps FE6J/FE8J/FE8U to `resources/fe-info/json/{game}/code.json`; FE7 is intentionally no-op. `LoadSymbolMap` merges through the `LoadSymbolMap` seam (made `internal` for cross-assembly testing) after shipped `asmmap_*` files load, caches per `game:region`, and uses shipped-wins semantics (`ContainsKey` guard) so bundled asmmap labels keep priority. Copied to the Avalonia + CLI outputs only (the WinForms disassembler uses its own `AsmMapFileAsmCache` path).
- `AoeRangeCore.cs` (Core, #1431; WF `AOERANGEForm`) — `ReadAoeRange`/`WriteAoeRange` (4+w*h; in-place else append+`RepointAllReferences`+ZERO old; orphan-refuse; ambient undo); Avalonia `AOERANGEView` manual-addr + dynamic decimal-cell grid (NumericUpDown `FormatString="0"` — Avalonia hex formats throw); NoList.
- `CStringCore.cs` (Core, #1445; WF `CStringForm`) — `ReadCString` (R/O; `TextForm.Direct` pointer decode = `getString`+`RevConvertSPMoji`+`@001F`-strip+`ConvertEscapeText`; unsafe⇒"") + `OldRegionLength` (`Padding2(len+1)`, EOF tail-clamp like `WriteBinaryData`) + `WriteCString` (encode raw `SystemTextEncoder.Encode`+NUL, in-place else `AoeRangeCore.Move` skeleton: early-orphan-guard→`RecycleAddress.WriteAmbient`+`RepointAllReferences`+parent-slot+ZERO old; ambient undo). Avalonia `CStringView` manual-addr + editable TextBox + Write; NoList.
- `EventScriptEditorCore.cs` (Core, #1435; WF `EventScriptInnerControl`) — editable `List<OneCode>` engine: `BuildFromRom`/`ScanLength` (es-driven, not `SearchEveneLength`), `Insert`(TERM-before, clone)/`Delete`/`Move{Up,Down}`/`InsertRange`(clone,skip-null)/`SetCodes`(clone,skip-null)/`NewCodeFromScript`(`CloneScriptDefaultByte`)/`ImportFromText`(guard null decode)/`Serialize`(auto-term, MAPTERM always-terminal)/`WriteAll` (`isSafetyOffset`+ALIGN4 gate src+dest; originalSize clamped to EOF; in-place else `FindFreeSpace`+`RepointAllReferences`+`NotifyChangePointer`, one ambient undo, **refs==0 REFUSAL**, byte-identical restore). Avalonia `EventScriptViewModel`/`EventScriptView` toolbar; event-kind ONE-SHOT via `StageEventKind` (no stale-leak in cached view); `WriteAll` via `CommitExternal` (no nested scope). #1585: ctor takes `EventScriptType` so `Serialize` appends the FAMILY terminator (Procs `End` 0000000000000000, not the FE event term — `WriteResult.NoTerminator` refusal if no family TERM in vocabulary); `ProcsScriptView` re-skinned onto the SAME engine (`EventScriptViewModel{ScriptType=Procs}`) — AI stays its own 16-byte-grid editor. `EventTemplateCore.TryGenerate{Button,BrowserTemplate}Codes`+`DisassembleToCodes` feed the template browser's "Send to Event Editor" (in-editor insert of placeholder-free templates via `EventScriptView.InsertCurrentTemplate`, gated on a loaded script). #1591: `EventEditorHostContext` (Core, R/O, `IEventEditorHostContext`=map-id+label allocator) ports the Alloc-Event host so the context-required browser templates substitute REAL labels/pointers when sent into an OPEN editor: `FindMapID` (over `EnumerateEventEntries`), `ResolveEndEvent/Player/Enemy` (first cond slot via `MapEventUnitCore`), `GetUnuseLabelID` (`EventScriptUtil.GetScriptLabelID` over the loaded list), `To{Pointer,UShort}ToString`; `EventTemplateCore.TryGenerateBrowserTemplate{,Codes}WithContext` + `ClassifyContextKind` (Cond/Preparation/CallEndEvent/Unknown). GATE HOLDS (no partial bytes): refuses (empty) when host==null, map-required-but-`TryGetMapID`-false (no silent map-0), Unknown family, or any residual placeholder post-substitution. `EventScriptView.BuildHostContext()`→`EventScriptTemplateView.SendToEditor`. #1592: EventCond-RECORD Alloc-Event side effects (the SEPARATE `InputFormRef.AllocEvent_EventTemplate` surface): `EventEditorHostContext.ResolveCallTemplate` (CALL_EndEvent→`toPointer(ResolveEndEvent)`+W2=0x03, REFUSE on NOT_FOUND map/end-event; CALL_1→literal 1) + `CounterReinforcementSideEffects` (B8=1/B9=255) + `IsEventPointerSurface` (category+condType NEWALLOC-EVENT gate: TURN N02/TALK N03·04·0D/OBJECT N06·08 not chest·shop/ALWAYS N01·0B·0D·0E). Avalonia `EventCondViewModel.ApplyAllocCallTemplate`/`ApplyCounterReinforcement` (W2→FlagId u16@+2, B8/B9→TurnStart/TurnEnd via category props, event-ptr→EventPtr@+4, undo-guarded) + `EventCondView` Alloc-Event panel (CALL EndEvent/CALL 1 + TURN-only Counter Reinforcement).
- `EventMoveDataFE7Core.cs` (Core, R/O, #1440; WF `EventMoveDataFE7Form`) — FE7 move-command walk: `IsEnableData`/`IsAppendedData` (WF-typo alias `IsAppnedData`)/`Stride` (9/0xC stride 2 = +B1/time byte; 0xA single-byte) + `WalkCommands` (one `AddrResult` row per command, stops at first non-enable). Single source of truth shared with `EventSubEditorHelper.ValidateMoveData` (fixes 0xA mis-skip). Avalonia `EventMoveDataFE7View` per-command list + B1/Time row shown live for 9/0xC (mirrors `B0_ValueChanged`); Write under ambient undo (correct offset gating, NOT the inverted WF guard); VM `GetListCount`=walked count so `--data-verify-full` covers every command.
- `EventUnitColorCore.cs` (Core, R/O, PURE, #1444; WF `EventUnitColorForm`) — `UNIT_COLOR` 4-slot picker: `Pack`/`Unpack` (`a|b<<4|c<<8|d<<12`; 4th=nibble d, fixes WF `JumpTo` c-bug) + `GetUNIT_COLOR` label (WF `InputFormRef` delegates). Avalonia `EventUnitColorView` real 4-combo picker (was placeholder); event editor `ScriptEditorHelper` "Pick..." `ShowDialog<uint?>` write-back + `ResolveDisplayName` label; ListParity NoList.
- `SongWaveConvertCore.cs`/`SongSoxConvertCore.cs` (Core, #1448; WF `SongInstrumentImportWaveForm`/`SongUtilDPCM`) — DirectSound wav-import ENCODE/preview side: `WavToDPCMByte` (verbatim WF lookahead DPCM encoder; decodes back via `SongDirectSoundWavCore.ByteToWavForDPCM`), `CalculateSNR`, `LoadWavS` (.s `.byte`/`.word`), `HasHqMixer` (m4a gate via `PatchDetection.SearchPatchBool`), `Convert`/`Preview` orchestrators (DPCM gated OFF unless HQ-mixer present); `SongSoxConvertCore.ConvertWaveBySox` runs the configured external `sox` (`Config.at("sox")`; no-op early-exit; clear error when unset — no bundled resampler). `SongDirectSoundWavCore.ImportSampleBytes` appends ready (raw|DPCM) sample bytes. Avalonia `SongInstrumentImportWaveView` real options dialog (was empty stub) returns bytes via `ShowDialog<byte[]?>`; Cancel = strict no-op; ListParity NoList.
- `WorkSupportUpdateDownloadCore.cs` (Core, #1454; WF `ToolWorkSupportForm`) — work/ROM-hack update download+apply pipeline (the half not ported by #1196): `ResolveDownloadUrl` (UPDATE_URL/UPDATE_REGEX with `@CHECK_URL`/`@DIRECT_URL` fallbacks + `EscapeURLToDecode`), `DownloadAndStage` (256-byte floor; raw-UPS copy vs `ArchSevenZip.Extract`+ported `CopyDirectory1Trim`; enumerate `*.ups`), `ApplyUpsAgainstOriginal` (**validate-ALL-before-write**: apply every UPS in memory first, write all `.gba` only if all succeed → no partial output; CRC mismatches collected as `Warnings`, not flattened). All network/extract/ROM-apply injected as delegates → offline tests. Avalonia `ToolWorkSupportViewModel`/`ToolWorkSupportView.Update_Click` now check the loaded hack's own `.updateinfo.txt` (was the editor's GitHub release): CheckReady guard, `WorkSupportUpdateCheckCore.Check`, force-update via `ToolWorkSupport_UpdateQuestionDialogView`, CRC32 vanilla-ROM auto-find in `ToolWorkSupport_SelectUPSView`, reopen via `MainWindow.LoadRomFile`. Deferred (non-goals): silent background auto-update, AutoFeedback telemetry, `DISABLE_CHEAT`.
- `SupportUnitAutoCollectCore.cs` (Core, ROM-MUTATING, #1455; WF `SupportUnitForm.AutoCollect`) — FE7/8 reciprocal-support mirroring: `RecomputePartnerCount` (B21=non-zero of 7 slots) + `AutoCollect`/`AutoCollectByTargetSupport` (resolve partner row via `SupportUnitNavigation.GetSupportAddrForUnitId`, scan 7 slots for owner uid, write init `+SUPPORT_LIMIT`/growth `+2*SUPPORT_LIMIT`; **full-row safety guard** = true max touched byte `targetAddr+(LIMIT-1)+2*LIMIT` AND 24-byte range, not WF base-only; ambient undo). Avalonia `SupportUnitEditorViewModel.WriteSupportUnit` runs it inside the View's `BeginUndoScope`; "Auto-adjust partner values" CheckBox default-on, **disabled in decomp mode** (source-backed path field-scoped, no reciprocal source write).
- `ImageTSAAnimeFrameEnumCore.cs` (Core, R/O, PURE, #1457; WF `ImageTSAAnimeForm`) — `EnumerateFrames(rom,tsaAnime)` lists ALL FRAMECOUNT frames per `tsaanime_` category at `base+i*12` (mirrors `ReInitPointer(pointer,count)`; FRAMECOUNT=col0, NAME=col1; ROM-explicit `isSafetyOffset`, EOF bounds-guard, no 20-cap). Shared by Avalonia `ImageTSAAnimeViewModel.LoadList` (was `LoadTSVResource1` frame-0-only) + `ListParityHelper.BuildImageTSAAnimeList` so frames 1..N-1 are reachable/editable; v1 View import fixed to WF slot order (img@+0, **palette raw**@+4, TSA@+8) + raw ExportPal/ImportPal.
- `StatusRMenuListCore.cs` (Core, R/O, #1459; WF `StatusRMenuForm`) — `TableCount` (6 on FE8, 5 otherwise; version gate) + `GetTablePointer` (0..5 → the six `status_rmenu*_pointer` roots) + `BuildTableList` (port of `Init`/`ListFounder`: BFS from `p32(root)` following the 4 directional pointers @+0/+4/+8/+12, dedup, stride 28, terminal nodes included) + `GetMenuName` (tid≤0x10 blank; first-line `\r\n` cut on the RAW `FETextDecode.Direct(rom,...)` decode BEFORE strip; reads ambient encoder, so R/O not strictly pure). Avalonia `StatusRMenuViewModel.SelectedTableIndex` + `StatusRMenuView` FilterComboBox now expose ALL up-to-6 RMenu tables (was unit-only + weak linear `+i*28`); `ListParityHelper.BuildStatusRMenuList` re-points to the same traversal.
- `ProjectRenameCore.cs` (Core, FILESYSTEM-MUTATING, FS-injectable, #1461; WF `ToolChangeProjectnameForm`) — real project-file rename (was an Avalonia no-op UI shell): `Validate` (refuse modified/virtual ROM, bad/empty/same name) + PURE `BuildPlan` (prefix-match every file whose name starts with the old project name → ROM + backups, suffix/ext preserved) + `ExecutePlan` (delete-then-move per dest; etc dir moved last) + `Rename` (resolves the `config/etc/<title>/` dirs via `U.ConfigEtcFilename`, returns the new ROM path). `IProjectRenameFileSystem`/`RealProjectRenameFileSystem` so tests run without disk; `U.IsBadFilename` ported to Core. Avalonia `ToolChangeProjectnameViewViewModel.TryRename` + `ToolChangeProjectnameView.OK_Click` reload via `MainWindow.LoadRomFile` (mirrors WF `ReOpenMainForm`+`LoadROM`).
- `PatchMetadataCore` clean-ROM-diff uninstall (Core, ROM-MUTATING, #1462; WF `PatchForm.UninstallPatchInner`) — `CollectPatchRegionsWithBytes`/`CollectPatchRegions`(out untraceableCount)/`RomContainsPatch`/`IsCompatibleRom`/`UninstallPatchWithCleanRom`: uninstall a prior-session BIN patch (no per-patch backup) by diffing vs a user-picked patch-free ROM. Patch-absence check is FAITHFUL to WF `SearchContainThisPatchBy` — `RomContainsPatch` memcmps each region's candidate bytes against the patch's OWN `.bin` bytes (`PatchRegion.PatchBytes`=WF `t.bin`), NOT the current ROM (so a candidate that still contains the patch but is edited elsewhere is REJECTED; JUMP regions PatchBytes=null⇒skipped). Gates BEFORE mutation: GBA-header (0xA0..0xB0) compat + PREFLIGHT size gate (clean ROM must cover every region, else Fail not partial-Ok) + patch-absence. CORRECTION-ONLY restore writes only DIFFERING bytes (batched `write_range`) so over-estimated region/JUMP lengths never clobber neighbours; EA/`$FREEAREA`/`$GREP` ⇒ PARTIAL report (never over-claim); ambient undo, byte-identical rollback. Wires the orphaned Avalonia `PatchFormUninstallDialogView` into `PatchManagerView` Uninstall (backup fast-path kept; `CanUninstall` no longer backup-gated).
- `DisASMArgGrepCore.cs` (Core, READ-ONLY, PURE, #1463; WF `DisASMDumpAllArgGrepForm.Grep`/`IsSearchRegister`) — register-flow "Disassembly Argument Grep" over cached disasm lines (was a flat case-insensitive substring grep with all 5 options dead): `IsSearchRegister` (`" mov"`/`" ldr"` gate + literal `" rN"` substring — case-SENSITIVE, prefix-wide like WinForms) + `NormalizeSearchFunction` (hex→`0x`-pointer via `U.atoh`/`toPointer`/`To0xHexString`, symbol pass-through) + `Grep` (anchor on a register-set, within `allowNumber` rows find the target call, emit the block from anchor→call, hide-call drops the call line, hide-unknown skips `'('` sets, re-anchor on closer set, `i=regLine` rewind on window overflow; `regLine==0` line-0 sentinel quirk preserved verbatim). Avalonia `DisASMDumpAllArgGrepView` now surfaces all 5 options (target function, r0-r8 register, allowed-rows 1..20 default 5, hide-function-call, hide-unknown-arg) bound to `DisASMDumpAllArgGrepViewModel`.
- `MonsterWMapProbabilityCore.cs` (Core, FE8-only, #1464; WF `MonsterWMapProbabilityForm`) — the 3 World Map Monster surfaces the Avalonia editor dropped: `LoadStageList`/`ReadStageMapId`/`WriteStageMapId` (stage spread, `monster_wmap_stage_1/2`, stride 1, count 0xB, Eirika/Ephraim) + `LoadProbabilityList`/`ReadProbabilityRow`/`WriteProbabilityRow`/`Sum`/`GetBasePointLabels` (per-base probabilities, `monster_wmap_probability_1/2`, stride 9, count 0xB, 9-cell SUM%) + `ReadSkirmishStartEvent`/`ReadSkirmishEndEvent`/`WriteSkirmishEvents` (`worldmap_skirmish_startevent/endevent`) + `GetWorldMapPointName` (port of WF `WorldMapPointForm.GetWorldMapPointName`, stride-32 record, textid@+28). rom-aware `isSafetyOffset`, never throws, ambient undo. Avalonia `MonsterWMapProbabilityViewerView` adds the 3 sections with Eirika/Ephraim filter+row sync, live SUM%, skirmish Jump→`EventScriptView` with `SetEventKind(isWorldMapEvent:true)`; route list builders registered in `ListParityHelper`.
- `ArenaEnemyWeaponCore.cs` (Core, READ-ONLY, #1465; WF `ArenaEnemyWeaponForm`) — Arena Enemy Weapon TWO-list seam: `BuildBasicList` (`arena_enemy_weapon_basic_pointer`, stride 1, 8) + `BuildRankupList` (`arena_enemy_weapon_rankup_pointer`, stride 1, 0x1A=26) + verbatim `GetBasicTypeName`/`GetRankupTypeName` (per-slot label+guidance+icon; 0xFF separators/terminator). Avalonia `ArenaEnemyWeaponViewerView` was basic-only — now a 2-tab editor (Basic/Rank-up) each with its own `AddressListControl`+type-label+guidance+WeaponId+undo-tracked Write; `ArenaEnemyWeaponViewerViewModel` adds the rank-up load/write/count path; `ListParityHelper` basic builder routed through `BuildBasicList` (rank-up parity pinned by `ArenaEnemyWeaponRankupParityTests`).
- `AddressListClipboardCore.cs` (Core, PURE, #1539; WF `InputFormRef.CopyToClipbord`/`ClipbordToPaste`/`SwapData`/`ClearData`) — the GUI-free serialize/parse + structural-edit byte helpers behind the shared Avalonia `AddressListControl`'s **opt-in** structural-edit context menu: `Serialize`/`BuildHeader`/`TryParse` (clipboard wire format `"{list}@{form}" + " " + ToString("X")` per byte, NO leading zero; paste rejects unless token-count==BlockSize+1 AND exact header AND every token a hex byte 0..0xFF — closes the `U.atoh` silent-truncation gap) + `BuildCleared`/`BuildSwap`. `AddressListControl.EnableStructuralEdit(blockSize, reload, writeProtectId00?, useSwap, useClear, clipboardListName, clipboardFormName)` appends Copy-block/Paste(Ctrl+V)/Swap Up(Ctrl+Up)/Swap Down(Ctrl+Down)/Invalidate(DEL) (idempotent; control owns a private `UndoService`; full-range `isSafetyOffset` gate; swap on `SelectedOriginalIndex±1` so it's filter-correct; byte-identical rollback). `SoundRoomViewerView` is the first consumer (block size=`sound_room_datasize`, FE6 header `AddressList@SoundRoomFE6Form`); 100+ copy-only editors stay untouched. Reuses the EXACT WF Japanese-source `R._` keys so NO new translate entries; drag-drop reorder deferred (swap covers it).
- `StatusGameOptionCore.cs` (Core, ROM-MUTATING, #1607; WF `StatusOptionForm` AddressListExpandsButton + `OnPreGameOptionExtendsWarningHandler`) — Status Screen Option (Game Option) 44-byte `status_game_option` list-expand: faithful clone of `MapSettingCore.ExpandMapSettingTable` (#1085) — `CountGameOptions` (validity = pointer @+40, cap `0x100`=`StructExportCore.status_options`) + `ExpandGameOptionTable` (`ExpandTableTo` Fill=First / Repoint=RawAndLdrAll / **FullZeroTerminatorRow=true** so terminator+40==0 stops the +40 scan; audit guard ≥1 slot+canonical-covered+≤`MaxPlausibleRepointSlots`; byte-identical length-aware fault restore #885/#923). Avalonia `StatusOptionView` ExpandListButton → NumberInputDialog + ported WF repoint warning (`ShowYesNo`) → one undo scope; `StatusOptionViewModel.LoadList`+`ListParityHelper.BuildStatusOptionList` caps 64→0x100 so expanded rows enumerate. Visibility gate (WF `show_gameoption_extends`) NOT ported (no Avalonia editor ports it — warning is the gate).
- `SummonsDemonKingExpandCore.cs` (Core, ROM-MUTATING, FE8-only, #1606; WF `SummonsDemonKingForm.AddressListExpandsEvent`) — Demon-King Summon list-expand: `IsEnabled` (FE8 pointer+count both set/safe), `ReadCountByte`, `Expand(rom,newCountByte,undo,out err)` grows the 20-byte `summons_demon_king` table to `newCountByte+1` rows via `DataExpansionCore.ExpandTableTo` (rows=`countByte+1`; read-side loops `i<=count`) THEN `write_u8(summons_demon_king_count_address,newCountByte)` under ambient undo; rejects corrupt(`>=100`)/≤current; `MapSettingCore` #885 snapshot+length-aware restore+`undo.list.Clear()` on any fault (covers resize-append). Avalonia `SummonsDemonKingViewerView` Expand-List button (FE8-gated, pre-dialog max/corrupt guard, `NumberInputDialog`).
- `SummonUnitExpandCore.cs` (Core, ROM-MUTATING, FE8-only, #1605; WF `SummonUnitForm.AddressListExpandsEvent`) — 2-byte `summon_unit_pointer` list-expand: clone of `StatusGameOptionCore` — `CountSummonUnits` (validity = `u8(addr)!=0`, cap `0x100`=WF `count>=257` reject) + `ExpandSummonUnitTable` (version!=8/ptr==0 gate; `ExpandTableTo` Fill=First / Repoint=RawAndLdrAll / **FullZeroTerminatorRow=true** so `u8(term)==0` stops the +0 scan — a 0xFFFFFFFF term's first byte 0xFF = phantom row; audit guard; byte-identical fault restore #885/#923) then rewrites the SPECIFIC hardcoded `+1` pointer refs + `count-1` bytes per FE8J(`is_multibyte`)/FE8U (`SummonUnitForm.cs:78-117` verbatim, incl. the 2 SkillSystems greps → `U.NOT_FOUND` skip when absent), each EOF-guarded via ambient undo. Avalonia `SummonUnitViewerView` ExpandListButton (FE8-only enabled gate in `LoadList`) → NumberInputDialog → one undo scope.
- `MapTileAnimation1ImageCore.cs` (Core, #1602; WF `MapTileAnimation1Form`) — Map Tile Animation Type 1 image surface (anime1 is the INVERSE of anime2: RAW 4bpp block at the entry's **+4** pointer, sized by **+2** u16 length, NOT LZ77): `CalcEntryHeight` (=`CalcHeight(256,len)`), `ResolvePaletteOffset` (parent map's `palette_plist`→PALETTE table via `MapChangeCore.PlistToOffsetAddr`), `RenderEntryImage` (R/O, `ByteToImage16Tile` 256×H), `ImportEntryImage` (single-PNG, **+2 length AUTHORITATIVE/unchanged** — reject unless `height==CalcEntryHeight(currentLen)`, remap→`EncodeDirectTiles4bpp`→`WriteRawToROM` to +4, snapshot byte-identical fault restore #885/#923, ambient undo), `ExportBatchTxt`/`ImportBatchTxt` (`.mapanime1.txt`; batch honours the explicit 3rd length column with truncate, all `<=0xFFFF` guarded, injected PNG save/load delegates keep Core decoder-free). Avalonia `MapTileAnimation1View` GbaImageControl preview + sample-palette combo + Export/Import/ExportAll/ImportAll. **Composited-map GIF (the #1602 deferred half):** `ResolveGifContext` (first map using the anime1 PLIST → OBJ/obj2/palette/config/mappointer offsets), `ReadFrameBytes` (RAW 4bpp `change_bitmap_bytes`), `ExportGif` composites each frame onto the chapter map via `MapRenderCore.RenderMapImage`'s new `animeOverlayBytes`/`ANIME1_OBJ_PATCH_OFFSET`(=8192, the WF `U.ArrayPatch` offset) → `GifEncoderCore.Encode` (`GameFrameSecToGifFrameSec` timing, IImage disposed per frame); R/O. ExportAll save dialog offers `.mapanime1.txt`|`.gif`.
- `AIScriptPointerJumpCore.cs` (Core, #1600; WF `AIScriptForm.ParamLabel_Clicked`) — AIScript per-param `POINTER_AI*` jump to the 5 AI sub-editors: `ClassifyArg` (5 ArgTypes → `AiPointerKind`), `AllocIfNeed` (WF AllocIfNeed; alloc a fresh 4-byte ASM block for null/broken Coordinate/Range/CallTalk via the `MapExitPointCore.NewAlloc` seam — Units/Tiles never alloc), `IsBrokenData` (Coordinate `u16(off+2)!=0`), `WritePointerIntoBytes` (PURE — pointer into opcode `ByteData`, NO ROM). Avalonia `AIScriptViewModel.{TryGetParamArg,GetParam*,ClassifyParam,ParamNeedsAlloc,ApplyPointerJump}` write the resolved/allocated pointer into the selected row's IN-MEMORY `OneCode.ByteData` (clone-replace like `UpdateRow`) so a later `WriteScript` serializes it and pending row edits survive; `AIScriptView` 5 `Param*Label` clickable affordances (Cursor=Hand + PointerPressed) prompt-before-alloc (reuses WF JP key, no new translate) then `Navigate<>` the matching sub-editor seeded at the pointer; NavigationTargets manifest gains the 5 jump rows. Sub-Views' `NavigateTo` direct-load the supplied addr (#1414).
- `MapEditorTilesetCore.cs` map-resize additions (Core, PURE, #1735; Avalonia Visual Map Editor) — `MAP_MIN_WIDTH=15`/`MAP_MIN_HEIGHT=10`/`MAP_MAX_HEIGHT=63` + `GetLimitMapWidth(h)` (height-indexed max-width table ported verbatim from WF `ImageUtilMap`) and `BuildResizedMapData(old,oldW,oldH,top,left,right,bottom,fillTile,out resized,out newW,out newH,out err)` — pure per-edge pad/crop of the `[u8 w][u8 h]`+`w*h` u16 map buffer (copies `old[x,y]→new[x+left,y+top]` where in-bounds, fills the rest with `fillTile`=0 like WF `MapEditorForm.MapSizeChange`), **rejecting (never clamping)** results below 15×10 or above the FE height-dependent width limit, exactly like WF `MapEditorResizeDialogForm.ChangeButton_Click` (main-map path). ROM mutation is NOT here: `MapEditorViewModel.ApplyMapResize` reuses `ApplyMapGrid`'s fault-safe `LZ77.compress`→`ImageImportCore.FindAndWriteData` (grown map → fresh free space, so it relocates instead of overwriting trailing data)→`rom.write_p32` repoint→swap-cache/dims-on-success pipeline; `MapEditorView` "Resize Map…" toolbar button opens the pre-existing `MapEditorResizeDialogView` (T/L/R/B padding) under one `_undo` scope. Main-map W/H live only in the decompressed buffer header (verified — no separate map-settings dimension store).
- `MapTmxCore.cs` Tiled map import/export (Core, PURE, no Avalonia/System.Drawing deps; #1387 `.tmx`, #1796 `.tmj`; Avalonia Visual Map Editor) — `ParseTmx(xml,…)`/`ParseTmj(json,…)` → row-major `ushort[]` MAR grid, and `SerializeTmx(mapData,tsx)`/`SerializeTmj(mapData,tsx)` + `SerializeTsx(...)` emit a Tiled project (map file references an external `.tsx`). GID↔MAR: `gid-1 == chipsetIndex == MAR>>2`; `gid 0` (empty) ↔ MAR 0 (so **textual GID round-trip is NOT claimed for empty cells** — round-trip equality is on the decoded MAR grid). Both parsers **never throw** and reject before allocating: `.tmx` uses a DTD-prohibited/`XmlResolver=null` reader + a `MAX_DECODED_BYTES` (64 KB) base64/inflate cap; `.tmj` (System.Text.Json, guarded `TryGetProperty`/`TryGet*` per the `DecompAssetValidatorCore` idiom) adds a 512 KB raw-length precheck + a `GetArrayLength()==w*h` check before building the tile grid. Both validate dims 1..64, a single `firstgid==1` tileset, and layer-dimension match; `.tmj` layer `data` may be a JSON GID array or a base64 string (reusing the shared gzip/zlib decoders). ROM mutation is NOT here — `MapEditorView` Export/Import (Tiled) toolbar buttons dispatch on the chosen filename's extension (import also content-sniffs a leading `{`) and apply via `MapEditorViewModel.ApplyMapGrid` under one `_undo` scope; blocked in decomp mode.
- `RandomMapGeneratorCore.cs` / `FEMapCreatorLauncherCore.cs` / `FEMapCreatorTilesetDiscoveryCore.cs` / `RandomMapGeneratorMarParserCore.cs` (Core, READ-ONLY external-process seam, #1978 Phase 1; future Avalonia/CLI Random Map Generator) — FEBuilderGBA NEVER vendors FEMapCreator; it only launches a user-selected EXISTING fully-qualified local Windows `.exe`, effectively executable native file (non-Windows, including dotted self-contained names such as `FE_Map_Creator.Cli`), or managed `.dll` (`DOTNET_HOST_PATH` host only when libc `access(X_OK)` confirms the current process may execute it, else bare `dotnet`; URLs, drive/root-relative shapes, non-executable native files, PATH lookup for the user path, and shells are rejected). Discovery rejects raw `..` before lexical normalization, then parses `tilesets list --json`, confines every image/data path under the effective root through physical resolution (so symlink/junction escapes fail closed), and requires both confined assets plus a complete valid 512-pixel-wide PNG IHDR/CRC. Generation accepts only FEMapCreator's `experimental` (default), `legacy`, and `hybrid` algorithms and runs `generate --width --height --tileset --algorithm --seed --output <private>.mar --format mar --require-complete --force` (+ optional `--assets-dir`) inside `Path.GetTempPath()/FEBuilderGBA-mapgen-<guid>` with a fixed 120000 ms timeout and 1000000-char per-stream capture cap; `finally` always best-effort deletes that exact temp directory and logs cleanup failure via `Log.Error`. CLI generation first resolves the selected name through the same discovery/compatibility gate and rejects physical/symlink/hard-link collisions between `--out` and the program/image/generation-data files before generation or publication. Non-zero external exits append bounded stderr, stdout, or the runner's containment/capture error (in that priority order) to the surfaced error, so GUI/CLI callers receive the actionable failure reason. The produced headerless signed-little-endian FEMapCreator MAR (`tileIndex*32`) is NEVER copied directly into FEBuilder — one fixed-size buffer is filled from one non-write-shared stream, with EOF and trailing-byte rejection, before `RandomMapGeneratorMarParserCore` requires exact `w*h*2` bytes, each value `>=0`, divisible by 32, and in chipset range, then converts via `raw/32 -> chipsetIndex -> MapEditorTilesetCore.ChipsetIndexToMar(chipsetIndex)` (golden pipeline: raw `0,32,992,1024,32736` → FEBuilder MAR `0,4,124,128,4092`). Public entry points never throw; failures surface as typed categories (`InvalidPath`, `HostUnavailable`, `ProcessStartFailed`, `TimedOut`, `OutputLimitExceeded`, `NonZeroExit`, `OutputMissing`, `ParseFailed`, `IncompatibleTileset`) with bounded stdout/stderr tails for UI/CLI diagnostics.
- `MapEditorView.axaml`/`.axaml.cs` `GenerateRandomMap_Click`/`MapTileset_Click` + `RandomMapOneClickService.cs` + `RandomMapBackendSelectorCore.cs` + `GenerateRandomMapWorkflow.cs` + `OptionsViewModel.cs`/`OptionsView.axaml(.cs)` "Random-Map Tileset Mapping" section (Avalonia one-click orchestration seam, #1978 Slice 3, hardened in the Slice 3 BLOCKED-review follow-up; replaces the retired modal `GenerateRandomMapDialog*` AND the retired standalone `MapTilesetMappingDialogViewModel.cs`/`MapTilesetMappingDialog.axaml(.cs)`) — the Generate button (no ellipsis) is true one-click: it never opens a dialog, reading the current map's dimensions, an inline replayable seed `TextBox`, and a `Randomize` button from the existing responsive upper-controls scroller (`MapRandomCommandToolbar`/`MapRandomSeedToolbar`). Per Plan v4 §4/§7, executable/assets setup AND per-fingerprint tileset-mapping discovery/save now live ENTIRELY inside the FEMapCreator section of `OptionsView`/`OptionsViewModel` — the ONLY Avalonia surface that may call `FEMapCreatorTilesetDiscoveryCore.DiscoverTilesets` (`OptionsViewModel.DiscoverTilesetsAsync`, wired only to its own async button click via `IsDiscoveringTilesets`/`CancelTilesetDiscovery`; construction, `Load()`, and `SetTilesetContext` never discover or launch a process) and persists a mapping via `TryCreateEntry`/`Upsert`/`SaveAll`/`Config.Save`. Map Editor's "Map Tileset…" button is now only a **shortcut**: it resolves the current map's `TilesetFingerprint` and calls `WindowManager.Instance.OpenModal<OptionsView>(owner, view => view.SetTilesetContext(fingerprint))` — it owns no dialog/discovery/persistence of its own, and Options without map context shows guidance text instead of guessing a fingerprint. `OptionsView`'s discovery button is itself cancellable (`DiscoverTilesetsAsync` cancels any prior in-flight run first; closing/detaching the Options view also cancels via `CancelTilesetDiscovery()`), so a started external discovery process is genuinely owned and terminated, never merely abandoned. On each Generate click, the handler snapshots ROM/map/grid write identity (`MapEditorViewModel.MapWriteIdentity`) and the current `TilesetFingerprint` (via `BuiltInRandomMapTilesetCore.TryResolveMapTileset`) before doing anything else; a `_generatingRandomMap` bool plus a `try/finally`-reset busy state (`SetRandomMapBusyState`) disables the Generate/Randomize/Map-Tileset buttons and the seed box and rejects duplicate submissions. Every exit path — success, every validation/backend failure, and cancellation — sets a deterministic `RandomMapStatusLabel` text (`Ready`/`Failed.`/`Cancelled.`/`Backend: {0}`) via local `Fail(message)`/`CancelStatus()` helpers, so a failure or cancellation never leaves the transient "Generating..." text displayed. `RandomMapOneClickService.GenerateAsync` runs off the UI thread: it first resolves the map's tileset (failing fast, before either backend or the mapping resolver is consulted, if the tileset itself can't be resolved), then calls an injectable `RandomMapMappingResolverDelegate` (default: reads `CoreState.Config` via `FEMapCreatorProfileCore.Validate` + `FEMapCreatorTilesetMappingStoreCore.Lookup` — filesystem-only, no process/network) and hands the result to the pure `RandomMapBackendSelectorCore.Select`, which — from `FEMapCreatorMappingLookupResult.Status` alone — picks `External` (mapping `Current`, empty notice) or `BuiltIn` (mapping `Stale`/`Invalid`, with a non-empty actionable `Notice` naming the mismatch reason; or mapping `NoMapping`, with an empty notice as the ordinary, unremarkable default). This `Notice` is carried on and surfaced from **every** built-in outcome — success, corpus-resolution failure, generator failure, AND cancellation — never only on success, so a stale/invalid mapping explanation is never silently dropped alongside an actionable built-in error. Once the external adapter (`RandomMapGeneratorCore.Generate`) is launched there is NO fallback to the built-in engine — any external launch/exit/parse/validation failure surfaces directly as a typed failure, and a cancellation observed after either backend attempt starts is discarded (never applied to the ROM) and reported as a distinct `RandomMapOneClickResult.Cancelled` outcome rather than a stale success or generic failure. Cancellation-aware execution is real, not cooperative-only: the external path's `RandomMapExternalGenerateDelegate` routes through `RandomMapGeneratorCore.Generate`'s cancellation-aware overload, which owns and terminates only its own external `Process` via `ProcessRunnerCore.Run`'s 6-arg cancellation-aware overload (falling back to a CLI-compatible synchronous 5-arg overload only when a caller injects a legacy `ProcessRunnerDelegate`); both the external and built-in paths re-check the token immediately after backend completion (in addition to the pre-launch check), and `MapEditorView` re-checks `cts.Token.IsCancellationRequested` a second time immediately before `ApplyGeneratedMapOnUiThreadAsync`, so a cancellation racing the UI-thread marshal can still never reach the ROM-mutating apply. The built-in path constructs the corpus/fingerprint via the Slice 1 `BuiltInRandomMapGeneratorCore.TryGenerateFromRom` seam using the displayed user seed and maps its typed `BuiltInRandomMapErrorCategory` (`InvalidInput`/`InsufficientSourceData`/`Cancelled`/`SearchExhausted`) to distinct localized `R._()` messages; the built-in path never launches a process, performs discovery, or touches the network. Whichever backend runs, the resulting `RandomMapGenerationOutcome.EffectiveSeed` — always directly replayable per the Slice 1 contract for the built-in engine, and the caller's own seed for the external adapter — is written back into the seed `TextBox` before the map is applied, so the displayed value always reproduces the shown layout. The one successful apply crosses to the UI thread exactly once via `GenerateRandomMapWorkflow.ApplyGeneratedMapOnUiThreadAsync`, which re-validates the snapshotted `MapWriteIdentity` AND re-resolves+compares the tileset fingerprint immediately before writing (rejecting without any ROM mutation, and best-effort reloading the cache from ROM, if either drifted — e.g. another window changed the ROM/selection, or the tileset's OBJ/PAL/CFG bytes were edited during generation), then delegates to `TryApplyGeneratedMap`, which opens exactly one `UndoService.Begin`/`Commit` transaction, keeps `Commit()` LAST so any post-write refresh/palette failure rolls the ROM back and reloads the VM cache from the reverted bytes, and never uniform-fills or applies an identity fallback on failure. Every dependency (`ProcessRunnerDelegate`/cancellation-aware runner, external/built-in generate delegates, the mapping resolver, tileset discovery) is injectable, so tests exercise the full decision matrix (no-mapping→built-in, current-mapping→external-only, stale/invalid-mapping→built-in+notice on success/failure/cancel, started-external-failure→no fallback, cancel-while-external-running→discarded/no-mutation, unresolvable-tileset→neither backend invoked, cancellation→neither backend invoked, typed built-in error→message mapping, duplicate-click/busy/finally, stale identity/fingerprint rejection, apply/undo/rollback/refresh, discovery busy/duplicate/cancel/exception, compact-viewport layout reachability) without a real FEMapCreator process. This engine/integration makes no gameplay/objective-validity claim — see the Slice 1 entry below for the underlying visual-coherence-only boundary.
- `BuiltInRandomMapModelsCore.cs` / `BuiltInRandomMapTilesetCore.cs` / `BuiltInRandomMapEdgeSignatureCore.cs` / `BuiltInRandomMapCorpusCore.cs` / `BuiltInRandomMapGeneratorCore.cs` (Core, READ-ONLY, PURE/no-ROM-mutation, #1978 Slice 1; independently designed clean-room engine — see `docs/ENGINEERING-NOTES.md` for provenance/legal boundary vs. the external FEMapCreator seam above) — a deterministic, in-process alternative to launching FEMapCreator, built entirely from data already resolvable inside the current ROM. `TilesetFingerprint.Compute(romVersion, objData, paletteData, configData)` hashes the ROM version plus the decoded OBJ/PAL/CFG bytes of the map's resolved tileset (SHA-256 over version tag + all three buffers) so two maps are "the same tileset" only when every rendered byte matches, never merely by PLIST index; `TryResolveMapTileset` raw-copies OBJ/CFG via the existing decompression seam and ALWAYS copies a fixed `2*16*16`-byte (512-byte) palette snapshot regardless of caller-supplied buffer size, and `IsMarRenderable` requires both a valid chipset index and `configData!=null && tsaBase+7 < configData.Length`. `BuiltInRandomMapCorpusCore.TryBuildCorpus` performs one pure, uncached, per-call scan of `MapSettingCore.MakeMapIDList(rom)` in ascending table-row order, silently skipping (never failing on) any entry that fails to resolve or whose fingerprint differs from the current map's, and accumulates cell `Frequency`/`BorderFrequency` (border = `x==0||y==0||x==width-1||y==height-1`) plus directional `HorizontalAdjacency`/`VerticalAdjacency` (west→observed-east, north→observed-south) into `SortedDictionary`/`SortedSet` structures for fully deterministic enumeration order; there is intentionally NO static/process-wide cache, so evidence never leaks across ROM instances or a re-loaded ROM (verified by `BuiltInRandomMapCorpusCoreTests.TryBuildCorpus_CrossRomIsolation_NoLeakageBetweenSeparateRomInstances`). `BuiltInRandomMapEdgeSignatureCore.TryComputeEdgeSignature` decodes a MAR value's four 16x16-metatile sub-tile boundary pixel (raw 4bpp index) sequences from its TSA config entry, honoring per-sub-tile h/v-flip bits before extraction, into a `Top`/`Bottom`/`Left`/`Right` signature; `HorizontallyCompatible`/`VerticallyCompatible` require the exact index-for-index match of the touching edge pair (west `Right` vs. east `Left`, north `Bottom` vs. south `Top`) — this is the sparse-data relaxation used when the strict model has too little directly observed adjacency. `BuiltInRandomMapGeneratorCore.Generate` validates dimensions against `MapEditorTilesetCore`'s existing min/max-width table, then **prefilters** `corpus.Candidates` through `BuiltInRandomMapTilesetCore.IsMarRenderable(candidate, corpus.ConfigData)` — preserving the corpus's ascending order — before any model/search construction, so a MAR value that can never legally render never consumes node budget or causes a spurious `SearchExhausted`; it requires at least 2 *renderable* candidates (else `InsufficientSourceData`, reporting both the renderable and raw candidate counts), and fails `InsufficientSourceData` unless the corpus offers strict adjacency evidence or at least 2 resolvable edge signatures over that renderable set. It then runs a bounded ladder of up to 2×4 deterministic restarts (Strict then EdgeRelaxed, each restart's internal-only RNG seed derived via `unchecked(baseSeed*397 + restart*31 + modelSalt + 0x9E3779B9)`), each an iterative (non-recursive) row-major backtracking search over a per-cell precomputed weighted-random candidate order drawn only from the renderable candidates, bounded by `ComputeNodeBudget(width,height)=min(1_000_000, max(10_000, width*height*64))` and checking `CancellationToken` every node — cancellation anywhere surfaces as `BuiltInRandomMapErrorCategory.Cancelled`, not an exception. **`EffectiveSeed` is the caller-supplied `seed` verbatim (directly replayable)** — calling `Generate` again with `result.EffectiveSeed` as the input `seed` reproduces the identical MAR grid/model/restart outcome; the per-restart derived values above are a private implementation detail and never surface as `EffectiveSeed`. `Model`/`RestartsUsed` remain in the result for diagnostics. A completed grid is accepted only if every cell is renderable, every adjacent pair is valid under the active model, and — whenever `>=3` renderable candidates are available — the quality gate additionally requires `>=3` distinct MAR values used and no single value occupying `>90%` of cells. **Source-identity condition (Plan v4 §2.6, absolute):** EVERY restart of EVERY attempt in the whole ladder — including its very last one — rejects a candidate grid that is sequence-identical to the caller-supplied `currentGrid` and keeps backtracking within that same attempt for a distinct completion; there is no final-attempt exception, so a from-scratch generation with `currentGrid==null` never exercises this rule at all (nothing to be identical to). If no attempt in the ladder ever finds a quality-gated, non-identical completion, `Generate` returns `SearchExhausted` rather than falling back to the source grid (or any other uniform/degenerate output) — there is no uniform-fill or identity success fallback anywhere in the ladder. Because real FE maps are always `>=15x10` (150 cells), the diversity/occupancy gates' `mars.Length>=3` guard is never the binding constraint in practice; a 1-2 cell "tiny map" is unreachable through the public API (`Generate` itself rejects `width`/`height` below `MapEditorTilesetCore.MAP_MIN_WIDTH`/`MAP_MIN_HEIGHT`), so no additional tiny-map carve-out exists or is needed beyond the `>=3`-candidate/`>=3`-cell diversity-gate scoping already described above. `BuiltInRandomMapGenerationResult.Mars` is always a freshly allocated defensive copy; `Generate` never mutates its `corpus` or `currentGrid` arguments. This engine intentionally makes no gameplay/objective-validity claim (chest/door/event reachability, etc.) — only visual tile-adjacency coherence per the model above. Covered by `FEBuilderGBA.Core.Tests`' `BuiltInRandomMap*CoreTests` (fingerprint determinism/difference, corpus ordering/filtering/cross-ROM isolation, strict/edge-relaxed models, determinism, quality gates, cancellation/budget, insufficient-source failures, no-input-mutation) using synthetic ROM/corpus fixtures — no copyrighted ROM data required. Slice 2 (`FEMapCreatorProfileCore.cs` / `FEMapCreatorTilesetMappingStoreCore.cs`, immediately below) added persisted external-tool setup and per-tileset mapping; Slice 3 (`RandomMapOneClickService.cs`/`RandomMapBackendSelectorCore.cs`, further below) wires this engine into the Avalonia one-click Map Editor Generate button, selecting it automatically whenever no `Current` external mapping exists.
- `FEMapCreatorProfileCore.cs` / `FEMapCreatorTilesetMappingStoreCore.cs` / `FileContentIdentityCore.cs` (Core, READ-ONLY on validate/lookup, #1978 Slice 2, staleness hardened in the Slice 2 review follow-up; Avalonia `OptionsViewModel`/`OptionsView` "FEMapCreator (Optional, External Tool)" section) — durable configuration for the external FEMapCreator seam above, stored as plain string values in the existing global `CoreState.Config` (no new persistence mechanism): `FEMapCreatorProfileCore.ExecutablePathConfigKey`=`"femapcreator_path"` and `AssetsRootConfigKey`=`"femapcreator_assets_root"`. `Validate(rawExecutablePath, rawAssetsRoot)` is pure/read-only (stats the filesystem only, never launches a process or touches the network) and returns a `FEMapCreatorSetupSnapshot` with `FEMapCreatorSetupStatus.NotConfigured` (blank path — a fully supported default), `.Configured` (normalized absolute path to an existing file, reusing `FEMapCreatorLauncherCore`'s internal `TryNormalizeAbsoluteLocalFile`/`TryNormalizeAssetsDirectory` plus an explicit `File.Exists` check that those helpers do not themselves perform; an empty assets root is valid and only validated when non-blank), or `.Invalid` with a specific reason (missing file, directory-as-file, non-existent assets root, relative/PATH-style path — no PATH search is ever performed). When `.Configured`, the snapshot also carries the executable's own content identity (`ExecutableSizeBytes`/`ExecutableLastWriteUtcTicks`/`ExecutableSha256`, computed via the shared `FileContentIdentityCore.TryCompute` stat+SHA-256 helper — the same helper used for the mapped tileset files below, avoiding duplicate hashing logic). `FEMapCreatorTilesetMappingStoreCore` persists a JSON array (`System.Text.Json`, key `"femapcreator_tileset_mappings"`) of `FEMapCreatorTilesetMappingEntry` records keyed by the Slice 1 `TilesetFingerprint.Value` (never a duplicate hash implementation) plus tileset name, image/generation-data path+size+mtime-ticks+SHA-256, and the executable path+size+mtime-ticks+SHA-256 plus normalized assets-root path active when the mapping was made. `LoadAll`/`SaveAll` never throw and silently skip structurally malformed individual entries (missing key/corrupt JSON/foreign schema, including entries recorded before the executable-identity fields existed) rather than discarding the whole list or crashing; `IsStructurallyValid` now also requires a non-blank `ExecutablePath`/`ExecutableSha256`. `TryCreateEntry(fingerprint, tilesetName, imagePath, generationDataPath, profile, out entry, out error)` takes the caller's already-validated `FEMapCreatorSetupSnapshot` and refuses (with an explicit error, no entry produced) unless `profile.Status == FEMapCreatorSetupStatus.Configured` — this closes the staleness bug class at the write path, not only at lookup. `TryCreateEntry`/`Upsert`/`Remove` remain pure functions over an immutable `IReadOnlyList<>` (no static cache — every call re-reads what the caller supplies). `Lookup(mappings, fingerprint, currentProfile)` re-stats and re-hashes the mapped image/generation-data files AND additionally requires `currentProfile` to be non-null, `.Configured`, and to match the entry's recorded executable path (case-insensitive on Windows via `FEMapCreatorLauncherCore.PathComparison`)/size/mtime/SHA-256 and normalized assets-root (blank remains a valid, matchable state) — any executable-path/content/assets-root drift versus the *current* profile, not merely the mapped tileset files, now returns `FEMapCreatorMappingStatus.Stale` with a specific actionable reason. Return values remain `NoMapping` (absent/empty fingerprint), `.Current` (both the mapped files' AND the current profile's identity fully match), `.Stale` (any of: mapped file missing/resized/content-changed, executable path/content changed, assets-root changed/cleared, or `currentProfile` itself not `.Configured`), or `.Invalid` (structurally broken entry) — a stale/invalid mapping is NEVER reported as `.Current`. There is deliberately no unsafe/legacy overload that skips the profile check. The Options view exposes executable/assets-root Browse+Clear controls, live-refreshes its status line as the user directly types into either textbox (via `TextChanged`, not only on load/Browse/Clear), and never invokes `FEMapCreatorTilesetDiscoveryCore` or launches a process merely from opening Options, loading config, editing text, or Browse/Clear actions. Per-fingerprint mapping creation/editing is now wired entirely inside the Options FEMapCreator section (Slice 3 and its BLOCKED-review follow-up, `OptionsViewModel.DiscoverTilesetsAsync`/`SaveTilesetMapping`/`SetTilesetContext`, immediately above) — Map Editor's "Map Tileset…" button is only a shortcut that opens Options with the current fingerprint pre-selected; this store/persistence layer itself is unchanged since Slice 2. `FileContentIdentityCore` internally splits into a cheap `TryStat` (size+mtime only) and `TryComputeHashOnly` (SHA-256 only); `TryCompute` (used directly and unconditionally by `Lookup`, always fresh/uncached) simply calls both. `Validate` gained an internal 3-arg overload accepting an optional, per-caller-owned `FEMapCreatorExecutableIdentityCache` (Slice 2 second review follow-up, finding #1): a small, explicitly NON-static, instance-scoped hash-reuse cache keyed by (normalized path, size, last-write-UTC ticks) so the Options view's on-every-keystroke live status refresh does not re-hash an unchanged executable on every character typed into either FEMapCreator textbox — it always re-stats (cheap) and only re-hashes when path/size/mtime actually differ from its last computation. `OptionsViewModel` owns exactly one such cache per instance (i.e. per Options dialog session; a freshly opened dialog always starts with an empty cache), threading it into `GetFEMapCreatorSetupSnapshot()`; the public 2-arg `Validate(rawExecutablePath, rawAssetsRoot)` overload is an unconditional pass-through to the 3-arg overload with `null`, so every other existing caller — most importantly `FEMapCreatorTilesetMappingStoreCore.Lookup`'s caller, which must always receive a freshly hashed profile — is completely unaffected. The cache exposes a test-only `HashComputeCount` counter (never read by production code) used to assert hash-reuse behavior. Covered by `FEMapCreatorProfileCoreTests`/`FEMapCreatorTilesetMappingStoreCoreTests`/`FEMapCreatorExecutableIdentityCacheTests` (Core, including executable-path-change/content-change/assets-root-change-or-clear/unchanged-profile/null-or-unconfigured-profile Stale cases, and hash-reuse-vs-rehash cases) and `FEMapCreatorOptionsConfigPersistenceTests`/`FEMapCreatorOptionsManualEditStatusTests` (Avalonia, including a rapid-manual-edit no-rehash regression test).
- `ToolInitWizardView.axaml` EndPage FEMapCreator optional row (Avalonia, #1978 Slice 4; no new Core API) — a purely informational, always-optional row added to the pre-existing 9-`TabItem` `ToolInitWizardView` wizard's final EndPage, never gating or required for `IsCompletedThroughStep6`/`ApplyAll()` wizard completion. It repeats the Plan v4 §7 disclaimer verbatim (`FEMapCreator is an independent utility credited to bwdyeti; FEBuilderGBA does not bundle, host, license, or guarantee it.`, reused from the pre-existing Options FEMapCreator section — same literal, same `ja.txt`/`zh.txt` translation entries, no duplicate keys) plus a "Built-in Experimental remains available immediately if you skip this or leave it unconfigured." notice, and exposes exactly two explicit-click-only actions: `OnOpenFEMapCreatorProjectPage_Click` (fixed-URL `https://github.com/laqieer/FEMapCreator` launch through Avalonia's cross-platform `TopLevel.Launcher.LaunchUriAsync`) and `OnOpenFEMapCreatorOptions_Click` (plain non-modal `WindowManager.Instance.Open<OptionsView>()`, no fingerprint context since the wizard has no loaded ROM/map). The project-page action checks both the missing-`TopLevel` and launcher-returns-`false` paths and renders a localized, row-local failure status instead of silently dropping the failure on browser/mobile heads. Neither handler runs on construction, page display, or Finish — proven by headless tests that inject the `RecordingNavigationService` fake (#1122) via `WindowManager.Instance.SetService`, assert zero calls from mere `new ToolInitWizardView()`, and click the detached project-page button to prove the inline error becomes visible. This row intentionally duplicates NO setup form: the executable/assets-root path fields and the per-current-fingerprint discovery/mapping action remain owned exclusively by the Options FEMapCreator section (Slice 2/3 above) per Plan v4 §4/§7 and the Slice 3 review's "Plan ownership mismatch" finding — the wizard only ever navigates there.

## Logging — `Log.*` vs `Log.*F` (string.Format) overloads

**Files:** `FEBuilderGBA.Core/Log.cs` (used by Core, Avalonia, CLI) and `FEBuilderGBA/Log.cs`
(the WinForms `Log` class, which shadows the Core one inside the WinForms assembly).

The base `Log.Debug/Notify/Error(params string[] args)` sink only does
`string.Join(" ", args)` — it has **no `string.Format`**. So `Log.Error("... failed: {0}", ex.Message)`
recorded the **literal `{0}` token** in the log instead of substituting the value (the value
was still appended after a space, just un-substituted), polluting every such diagnostic line (#1637).

Fix: real `Log.DebugF/NotifyF/ErrorF(string fmt, params object[] args)` overloads do an actual
`string.Format(InvariantCulture, fmt, args)`. They route through the existing join sink with the
single formatted string. `FormatSafe` never throws from a logging call — a malformed format string
(bad/extra placeholder) falls back to the format string **with every `{N}` token stripped to `{}`**
plus the joined args, so the literal-placeholder bug can never be reintroduced via the fallback.
`DebugF` keeps `[Conditional("DEBUG")]` parity with `Debug`.

**Convention:** any `Log.*` call carrying a `{N}` placeholder MUST use the `*F` overload.
The `FEBuilderGBA.Tests/Unit/LogFormatPlaceholderTests` regression guard scans all four production
projects (Core, Avalonia, CLI, WinForms), strips comments, walks balanced parens (so it catches
multi-line calls), and FAILS the build if a plain `Log.Error/Notify/Debug(...)` carries a literal
`{N}` placeholder, or if a `Log.*F(...)` references a placeholder index its argument list cannot
satisfy. Runs in `check.yml` on every PR.

## Table Expansion Helpers (full prose)

**File:** `FEBuilderGBA.Core/DataExpansionCore.cs`

Cross-platform helpers for growing pointer-based ROM tables:
- `ExpandTable(rom, ptr, entrySize, currentCount)` — adds **one** entry; uses
  `0xFF` recycling for the old region.
- `ExpandTableTo(rom, ptr, entrySize, currentCount, newCount)` — grows to a
  specific row count; mirrors WinForms `InputFormRef.ExpandsArea(ExpandsFillOption.NO, ...)`:
  copies existing rows verbatim, zero-fills new rows, writes a `0xFFFFFFFF`
  terminator at `newBase + newCount * entrySize` (pointer-first scan stop),
  wipes the old region with `0x00`, repoints `CoreState.CommentCache` +
  `LintCache` entries via `IEtcCache.RepointEtcData`. Used by Avalonia
  `ImageMapActionAnimationViewModel.ExpandList` (#501).
- `RepointAllReferences(rom, oldBase, newBase, undo)` — opt-in, all-reference
  rescan that repoints EVERY reference to a moved table base: raw 32-bit
  pointers (`U.GrepPointerAll`) **and** ARM Thumb LDR literal-pool loads
  (`U.GrepPointerAllOnLDR`, ported to Core with EOF-safety guards in #781).
  De-duplicates the combined slot list via a `HashSet` (a valid LDR slot is
  also a raw hit), writes only literal/pointer slots via `rom.write_p32`
  (ambient-undo or the passed `undo`), and refuses safely (returns 0, no throw)
  on a no-reference ROM or a danger-zone (0x0–0x200) base. Mirrors WF
  `MoveToFreeSapceForm.SearchPointer`'s repoint minus the WinForms UI dialogs /
  event-aware `GrepPointerAllOnEvent` pass / `IsFixedASM` ASM-code guard
  (`InputFormRef`-dependent — out of scope). `ExpandTable`/`ExpandTableTo` keep
  their correct single-slot repoint for unshared tables.

Known WF parity gap in `ExpandTable`/`ExpandTableTo` (documented in XML doc, no
follow-up issue filed): forward-only cache repoint (rollback does NOT reverse it
— matches WF). The LDR-pointer rescan gap is now closed via the opt-in
`RepointAllReferences` helper (#781).
