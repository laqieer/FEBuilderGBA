# Decomp Project Mode — Feature Inventory & Release Status

This document enumerates the **decompilation ("decomp") project mode** feature set in
FEBuilderGBA, the issues/PRs that landed each slice, and — importantly — its **release
visibility** relative to tagged builds.

It exists to answer two recurring questions tracked in
[#1150](https://github.com/laqieer/FEBuilderGBA/issues/1150):

1. **What does decomp mode actually cover today?** (see the
   [round-trip coverage matrix](../README.md#round-trip-coverage-matrix) and the summary
   below)
2. **Where does this work live — `master`, or a tagged release?**

> **Keeping this current:** when a decomp-mode follow-up lands a new capability (a CLI flag, a
> source writer, a validator, or a GUI routing change), add or move its row in the
> [feature inventory table](#feature-inventory-by-capability) and reconcile the
> [honest residuals](#honest-residuals-not-yet-covered) below — and cross-check the
> [README "Decomp Project Support" section](../README.md#decomp-project-support-preview) and
> [`docs/cli-reference.md`](cli-reference.md) so the three stay consistent. Do **not** hand-edit
> the README `<!-- decomp-audit-matrix -->` block — it is generated by `--decomp-audit
> --format=md` and byte-locked by a cross-project test.

## Release status (read this first)

> **The decomp feature set currently lives on `master`, AHEAD of any tagged release.**

The latest functional tagged release at the time of writing is **`ver_20260204.22`**
(2026-02-04). **All** of the decomp project-mode work below landed on `master` *after*
that tag, so it is **not part of any existing tagged release / GitHub Release yet**.

FEBuilderGBA follows a periodic `ver_YYYYMMDD.NN` release cadence (snapshot tags, roughly
every few months). Until a new tag is cut, the only way to use the decomp features is to
build from `master` (or from a CI artifact). This document does **not** claim these
features ship in any released build — it makes the master-vs-release gap explicit so the
state is not mistaken for "released."

Cutting a tagged release is an outward-facing maintainer action and is intentionally **not**
performed automatically by this work.

## Coverage summary

The decomp round-trip coverage matrix classifies each editor/action into one of five tiers.
Generate the live counts with:

```bash
FEBuilderGBA.CLI --decomp-audit --summary
```

The summary prints per-tier counts, the total row count, an explicit `Unclassified = N`
line, and the size of the maintained editor inventory
(`DecompRoundTripAuditCore.ExpectedDecompEditors`). The full table is generated by
`--decomp-audit --format=md` and mirrored into the
[README coverage matrix](../README.md#round-trip-coverage-matrix) (a cross-project test
keeps the two byte-identical).

> **Honest scope:** the matrix is *complete relative to the maintained audit inventory* —
> a maintained classification matrix, **not** an exhaustive byte-level runtime round-trip
> proof, and **not** a claim that every FEBuilder editor everywhere is source-writable.
> Full byte-level round-trip editing of every format remains **partial by design** (see the
> non-goals in #1150). The tiers honestly record what is source-backed, what migrates via an
> exporter, and what still needs a manual source edit.

## Feature inventory (by capability)

Each row lists the capability, the tracking issue, and the PR(s)/commit(s) that landed it on
`master`.

| Capability | Issue | Landed via |
| --- | --- | --- |
| **Open + preview** a decomp project directory (manifest/heuristic detection, read-only built-ROM preview, `--project`, GUI *Open Decomp Project…* + badge) | #1129 | PR #1136 (`85caeab72`) |
| **Address → source symbol resolution** (`.map`/ELF/`.sym`/JSON merged over asmmap; `--resolve-addr`; Pointer Tool "What is this address?") | #1130 | PR #1138 (`4920b07ba`) |
| **Diff-to-source migration assistant** (`--migrate-diff`; range coalescing, nearest-symbol + category + confidence; READ-ONLY) | #1131 | PR #1139 (`14581b877`) |
| **Source-backed table writer (items)** (`--write-source`; in-place C-array element rewrite, no ROM mutation) | #1132 | PR #1142 (`7ade9df28`), gate fix #1143 (`534be0510`) |
| **Asset export to source-tree pipelines** (`--export-asset` palette/graphics/map/text; indexed PNG + `.pal` + `.mar` + sidecars) | #1133 | PR #1140 (`3b44e640d`) |
| **In-app build / reload** (`--build-project`; run the project's build, reload the built ROM) | #1134 | PR #1144 (`5945d33b9`) |
| **More source writers + JSON path + signed/multi-field** (units (alias `characters`), classes; JSON element rewrite; signed fields; repeatable `--field`/`--value`; full-document JSON validation; width-aware signed no-op) | #1141 | PR #1145 (`a2b1805e4`), fixes `e665b84e4`, `9ffc14860`-era hardening |
| **Source-backed chapter / map_settings editing** (scalar fields source-writable; pointer-field + map-asset guards in GUI; `.mar` tile-layout source-backed) | #1148 | PR #1346 (`8d82c5f50`), `969e377c9`, `9f7d8254f` |
| **Support data source writers** (`support_units`, `support_attributes`, `support_talks`; byte-offset field names) and **shop GUI guard** (block ROM-only shop writes in decomp mode) | #1149 | PR #1159 (`f9b1785e9`), `91dec9e79` |
| **Source-tree shop list export** (`--export-asset --kind=shop`; reviewable EA `.event` migration artifact for variable-length `ITEM_NONE`-terminated `u16` lists) | #1149 | PR #1348 (`94edfd161`) |
| **Round-trip coverage audit** (`--decomp-audit`; maintained matrix + completeness summary), **Nightmare `.nmm` schema bridge** (`--nmm-to-manifest` / `--manifest-to-nmm`; pointer/odd-size fields flagged, never dropped), **asset import validation** (`--validate-asset`; structural + index-level PNG/JASC/`.mar`, never reads a ROM) | #1150 | PR #1160 (`ff1dec009`), completeness + release-visibility follow-up #1150 (this doc) |
| **Source-backed shop-list writer** (`--write-shop`; in-place rewrite of a variable-length `ITEM_NONE`-terminated `u16` shop list in source via a manifest `u16-list` list-owner + `DecompShopSourceResolver`; resolved by `--symbol=<name>` or `--shop-addr=<offset>`; literal-numeric form) | #1347 | PR #1351 (`6fbe4c4ee`) |
| **Symbolic `ITEM_*` shop lists** (`--write-shop` re-serializes a list that uses `ITEM_*` macro names symbolically, resolving id↔macro via `DecompConstantResolver` from the constants header; item-id-only, unknown/ambiguous macros refused — no clobber) | #1354 | PR #1356 (`7f3bcc00e`) |
| **GUI shop save → source routing** (Avalonia Item Shop editor Write / Append Slot / Remove Last Slot route to the owning decomp source list when the shop resolves to a manifest `u16-list` owner — covering **both** literal raw-hex lists and resolvable symbolic `ITEM_*` item-id-only lists (#1354), while refusing nonzero quantities, unknown/ambiguous macros, and unowned/unresolved shops; those `NotRouted` cases keep the #1159 ROM-only/manual guard + `--export-asset --kind=shop`) | #1347 | PR #1352 (`30106da38`) |
| **Portrait PACKAGE validator** (`--validate-asset --kind=portrait-package --path=<dir>`; multi-file package — required composite sheet, indexed PNG structural checks, 128×112 slot geometry, 4bpp palette cap, sheet↔`.pal` palette consistency; READ-ONLY, never reads a ROM; `--project` root-confined) | #1350 | PR #1353 (`1659f356b`) |
| **Portrait PACKAGE source write-back + round-trip** (`--import-asset` / `--roundtrip-asset --kind=portrait-package`; identity write-back of a validated 128×112 sheet + name-matched JASC sidecar into an **unambiguous project-confined owner** — clean/empty dest or `--overwrite` an existing single-package owner, multi-PNG/different-layout dest refused (`AMBIGUOUS_OWNER`); structural round-trip vs a REQUIRED `--expect` baseline (the oracle, no self-compare); reuses the #1350/#1353 validator; ROM-free, never mutates the preview ROM. Residual: NO ROM byte-pin — no canonical ROM→128×112-sheet builder, so the preview ROM is never the source of truth) | #1374 | (this PR) |
| **Map-change overlay source export/import/round-trip/ROM-verify** (`--export-asset` / `--import-asset` / `--roundtrip-asset` / `--verify-asset --kind=mapchange`; the raw uncompressed `u16` overlay tile-data block — source-level structure-exact identity + read-only byte-exact ROM compare; not the `.mar` layout, not the 12-byte change-record chain) | #1355 | PR #1357 (`92962bbde`) |
| **Map tile-animation-2 palette source export/import/round-trip/ROM-verify** (`--export-asset` / `--import-asset` / `--roundtrip-asset` / `--verify-asset --kind=mapanime2pal`; the raw uncompressed `u16` palette block of `count` 15-bit GBA colors reached by each anime-2 entry's `+0` pointer — structural twin of `mapchange` with a single `count` descriptor; source-level structure-exact identity + read-only byte-exact ROM compare; not the anime-2 entry/PLIST table, not LZ77) | #1360 | PR #1365 (`b8a0b5700`) |
| **OBJ tileset LZ77 decompressed-payload source export/import/round-trip/ROM-verify** (`--export-asset` / `--import-asset` / `--roundtrip-asset` / `--verify-asset --kind=objtiles`; the DECOMPRESSED 4bpp payload of the OBJ LZ77 tile block — decompressed-payload equivalence, NOT compressed-stream byte identity (FEBuilder's LZ77 packer is non-canonical so the build re-compresses); `--addr` is the dereferenced OBJ LZ77 stream address; FE7 obj2 split out of scope; not chipset TSA/config, not tile animations 1/2) | #1371 | (this PR) |
| **Voicegroup source-macro-asm export** (`--export-voicegroup`; a FEBuilder M4A instrument set → reviewable `sound/voicegroups/voicegroupNNN.s` using `asm/macros/music_voice.inc`. Supported voice types emit the exact macros (`voice_directsound`/`_no_resample`/`_alt`, `voice_square_1`/`_2`, `voice_programmable_wave`, `voice_noise`, `voice_keysplit`/`_all`); keysplit/drum sub-voicegroups + DirectSound sample pointers emit valid raw `0x08XXXXXX` macro args + an unresolved-pointer diagnostic (no guessed symbol, sub-table NOT inlined); `0x18`/unknown → commented placeholder + diagnostic, never a wrong macro. READ-ONLY, never mutates the ROM, project-root-confined; EXPORT/source-helper only, not a byte-pinned round-trip or M4A re-assembler) | #1362 | PR #1368 (`670516783`) |

CLI usage for every flag above is documented in
[`docs/cli-reference.md`](cli-reference.md) and the
[README "Decomp Project Support" section](../README.md#decomp-project-support-preview).

## Honest residuals (not yet covered)

These remain **manual** or **ROM-only** in decomp mode — recorded here and in the coverage
matrix so the gaps are visible rather than implied-away:

- **Full byte-level round-trip editing of every format is not claimed.** The matrix tiers
  are honest: `ManualMigration` / `RomOnlyUnsupported` rows still require a hand-edit of the
  source (or are ROM-only). No format has byte-pinned ROM re-insertion of an edited asset —
  source-backed writers rewrite the *source* and flag the project **needs rebuild**.
- **Raw pointer-heavy map binaries stay manual / export-only** — the LZ77 map-asset binaries
  (OBJ tileset, chipset TSA/config, **tile-animation-1 graphics**, the **tile-animation-2
  ENTRY/PLIST table** + the non-palette anime-2 record/provenance chain) and the **12-byte
  map-change RECORD chain** (terminator / flag-ID / PLIST metadata) have no in-place source
  writer; they stay guarded in decomp mode and migrate via `--export-asset` where an exporter
  exists. *(The map-change **overlay tile-data block** is now source export/import/verify-backed
  (#1355), and the dereferenced **tile-animation-2 PALETTE body** is now source
  export/import/verify-backed (#1360) — see the feature-inventory rows above — but the anime-2
  entry/PLIST table that points at the palette, and the 12-byte change-record chain, are not.)*
- **Shop residuals after `--write-shop` (#1351/#1356):** shop **lists** are now source-backed
  in place (literal-numeric and symbolic `ITEM_*`), so they are **no longer** an unconditional
  manual residual. The remaining residual is the **unresolvable** case: a shop reached via a
  scattered hensei/worldmap/**event-cond** pointer with **no manifest `u16-list` owner / no
  named source symbol**, an unnamed shop, or a source list containing an **unknown/ambiguous
  macro** — these degrade to the `--export-asset --kind=shop` EA `.event` migration artifact
  (a reviewable migration aid, **not** a byte-pinned ROM round-trip). Symbolic lists are
  item-id-only (non-zero quantity is refused).
- **Hyssopi map-editor import stays unsupported (investigated, blocked).** Importing
  map layouts authored in Hyssopi's Fire Emblem Tile Map Editor into source-owned
  `.mar` layouts was investigated (#1361) and is a **documented safe failure**: Hyssopi
  exports an opaque tile-**hash** grid, but `.mar` needs each cell's raw GBA `u16`
  tile index (`< 0x2000`), and the hash → GBA-`u16`-index crosswalk is undocumented and
  game/tileset-version-dependent. An automatic converter would have to guess that
  crosswalk, silently producing structurally-valid-but-wrong maps — which the #1361
  non-goals forbid. The supported interim path remains FEBuilder's own
  `--export-asset --kind=map` / `.mar` import. See
  [docs/DECOMP-HYSSOPI-IMPORT-FINDINGS.md](DECOMP-HYSSOPI-IMPORT-FINDINGS.md).
- **The `.nmm` schema bridge is a schema aid, not a writability path** — generating a
  manifest `tables[]` entry from `.nmm` does not make a table source-writable; pointer /
  var-length / odd-size fields are flagged `unsupported`, never silently treated as writable.
- **`--validate-asset` is structural + index-level** — it checks PNG color type / tile
  alignment / palette size / in-range pixel indices, JASC `.pal` headers, and `.mar` length
  vs sidecar. The multi-file portrait *package* is now covered by
  `--validate-asset --kind=portrait-package` (#1353: composite-sheet slot geometry + 4bpp
  palette cap + sheet↔`.pal` palette consistency) — but validation is **structural only**: it
  is **not** package write-back and does **not** assert semantic frame order across files.
- **The audit matrix is a maintained classification, not exhaustive runtime proof.** It is
  complete *relative to the maintained inventory* (`ExpectedDecompEditors`), enforced by a
  test that fails if a registered editor is missing or an unregistered editor appears. There
  is no real `fireemblem8u` / `fe6` decomp tree in this repo — the source-write, asset, and
  validation paths are exercised against **synthetic fixtures**, not a checked-in decomp tree.

## See also

- [#1129](https://github.com/laqieer/FEBuilderGBA/issues/1129) — decomp project mode tracker
- [#1150](https://github.com/laqieer/FEBuilderGBA/issues/1150) — round-trip audit / NMM bridge / asset validation
- [#1347](https://github.com/laqieer/FEBuilderGBA/issues/1347) — source-backed shop-list writer (PR #1351) + GUI shop save routing (PR #1352)
- [#1354](https://github.com/laqieer/FEBuilderGBA/issues/1354) — symbolic `ITEM_*` shop lists (PR #1356)
- [#1350](https://github.com/laqieer/FEBuilderGBA/issues/1350) — portrait PACKAGE validator (PR #1353)
- [#1355](https://github.com/laqieer/FEBuilderGBA/issues/1355) — map-change overlay source export/import/round-trip/ROM-verify (PR #1357)
- [#1360](https://github.com/laqieer/FEBuilderGBA/issues/1360) — map tile-animation-2 palette source export/import/round-trip/ROM-verify
- [#1371](https://github.com/laqieer/FEBuilderGBA/issues/1371) — OBJ tileset LZ77 decompressed-payload source export/import/round-trip/ROM-verify (this PR)
- [#1361](https://github.com/laqieer/FEBuilderGBA/issues/1361) — Hyssopi map import investigation (documented safe failure; see [DECOMP-HYSSOPI-IMPORT-FINDINGS.md](DECOMP-HYSSOPI-IMPORT-FINDINGS.md))
- [README → Decomp Project Support](../README.md#decomp-project-support-preview)
- [docs/cli-reference.md](cli-reference.md)
