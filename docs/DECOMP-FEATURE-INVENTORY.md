# Decomp Project Mode — Feature Inventory & Release Status

This document enumerates the **decompilation ("decomp") project mode** feature set in
FEBuilderGBA, the issues/PRs that landed each slice, and — importantly — its **release
visibility** relative to tagged builds.

It exists to answer two recurring questions tracked in
[#1150](https://github.com/laqieer/FEBuilderGBA/issues/1150):

1. **What does decomp mode actually cover today?** (see the
   [round-trip coverage matrix](../README.md#round-trip-coverage-matrix) and the summary
   below)
2. **Where does this work live — `master`, or a tagged release?**

## Release status (read this first)

> **The decomp feature set currently lives on `master`, AHEAD of any tagged release.**

The latest functional tagged release at the time of writing is **`ver_20260204.22`**
(2026-02-04). **All** of the decomp project-mode work below landed on `master` *after*
that tag, so it is **not part of any existing tagged release / GitHub Release yet**.

FEBuilderGBA follows a periodic `ver_YYYYMMDD.NN` release cadence (snapshot tags, roughly
every few months). Until a new tag is cut, the only way to use the decomp features is to
build from `master` (or from a CI artifact). This document does **not** claim these
features ship in any released build — it makes the master-vs-release gap explicit so the
state is not mistaken for "released."

Cutting a tagged release is an outward-facing maintainer action and is intentionally **not**
performed automatically by this work.

## Coverage summary

The decomp round-trip coverage matrix classifies each editor/action into one of five tiers.
Generate the live counts with:

```bash
FEBuilderGBA.CLI --decomp-audit --summary
```

The summary prints per-tier counts, the total row count, an explicit `Unclassified = N`
line, and the size of the maintained editor inventory
(`DecompRoundTripAuditCore.ExpectedDecompEditors`). The full table is generated by
`--decomp-audit --format=md` and mirrored into the
[README coverage matrix](../README.md#round-trip-coverage-matrix) (a cross-project test
keeps the two byte-identical).

> **Honest scope:** the matrix is *complete relative to the maintained audit inventory* —
> a maintained classification matrix, **not** an exhaustive byte-level runtime round-trip
> proof, and **not** a claim that every FEBuilder editor everywhere is source-writable.
> Full byte-level round-trip editing of every format remains **partial by design** (see the
> non-goals in #1150). The tiers honestly record what is source-backed, what migrates via an
> exporter, and what still needs a manual source edit.

## Feature inventory (by capability)

Each row lists the capability, the tracking issue, and the PR(s)/commit(s) that landed it on
`master`.

| Capability | Issue | Landed via |
| --- | --- | --- |
| **Open + preview** a decomp project directory (manifest/heuristic detection, read-only built-ROM preview, `--project`, GUI *Open Decomp Project…* + badge) | #1129 | PR #1136 (`85caeab72`) |
| **Address → source symbol resolution** (`.map`/ELF/`.sym`/JSON merged over asmmap; `--resolve-addr`; Pointer Tool "What is this address?") | #1130 | PR #1138 (`4920b07ba`) |
| **Diff-to-source migration assistant** (`--migrate-diff`; range coalescing, nearest-symbol + category + confidence; READ-ONLY) | #1131 | PR #1139 (`14581b877`) |
| **Source-backed table writer (items)** (`--write-source`; in-place C-array element rewrite, no ROM mutation) | #1132 | PR #1142 (`7ade9df28`), gate fix #1143 (`534be0510`) |
| **Asset export to source-tree pipelines** (`--export-asset` palette/graphics/map/text; indexed PNG + `.pal` + `.mar` + sidecars) | #1133 | PR #1140 (`3b44e640d`) |
| **In-app build / reload** (`--build-project`; run the project's build, reload the built ROM) | #1134 | PR #1144 (`5945d33b9`) |
| **More source writers + JSON path + signed/multi-field** (units (alias `characters`), classes; JSON element rewrite; signed fields; repeatable `--field`/`--value`; full-document JSON validation; width-aware signed no-op) | #1141 | PR #1145 (`a2b1805e4`), fixes `e665b84e4`, `9ffc14860`-era hardening |
| **Source-backed chapter / map_settings editing** (scalar fields source-writable; pointer-field + map-asset guards in GUI; `.mar` tile-layout source-backed) | #1148 | PR #1346 (`8d82c5f50`), `969e377c9`, `9f7d8254f` |
| **Support data source writers** (`support_units`, `support_attributes`, `support_talks`; byte-offset field names) and **shop GUI guard** (block ROM-only shop writes in decomp mode) | #1149 | PR #1159 (`f9b1785e9`), `91dec9e79` |
| **Source-tree shop list export** (`--export-asset --kind=shop`; reviewable EA `.event` migration artifact for variable-length `ITEM_NONE`-terminated `u16` lists) | #1149 | PR #1348 (`94edfd161`) |
| **Round-trip coverage audit** (`--decomp-audit`; maintained matrix + completeness summary), **Nightmare `.nmm` schema bridge** (`--nmm-to-manifest` / `--manifest-to-nmm`; pointer/odd-size fields flagged, never dropped), **asset import validation** (`--validate-asset`; structural + index-level PNG/JASC/`.mar`, never reads a ROM) | #1150 | PR #1160 (`ff1dec009`), completeness + release-visibility follow-up #1150 (this doc) |

CLI usage for every flag above is documented in
[`docs/cli-reference.md`](cli-reference.md) and the
[README "Decomp Project Support" section](../README.md#decomp-project-support-preview).

## Honest residuals (not yet covered)

These remain **manual** or **ROM-only** in decomp mode — recorded here and in the coverage
matrix so the gaps are visible rather than implied-away:

- **Full byte-level round-trip editing of every format is not claimed.** The matrix tiers
  are honest: `ManualMigration` / `RomOnlyUnsupported` rows still require a hand-edit of the
  source (or are ROM-only).
- **Variable-length / pointer / raw-binary data** — shops (in-place editing), chapter
  event/difficulty pointer fields, and the raw LZ77 map-asset binaries (OBJ tileset, chipset
  TSA/config, tile animations, map-change overlay) have no in-place source writer; they
  migrate via `--export-asset` where an exporter exists.
- **The `.nmm` schema bridge is a schema aid, not a writability path** — generating a
  manifest `tables[]` entry from `.nmm` does not make a table source-writable; pointer /
  var-length / odd-size fields are flagged `unsupported`, never silently treated as writable.
- **`--validate-asset` is structural + index-level** — it checks PNG color type / tile
  alignment / palette size / in-range pixel indices, JASC `.pal` headers, and `.mar` length
  vs sidecar. The full portrait *package* (multi-frame sheet ordering across files) is a
  documented residual.
- **The audit matrix is a maintained classification, not exhaustive runtime proof.** It is
  complete *relative to the maintained inventory* (`ExpectedDecompEditors`), enforced by a
  test that fails if a registered editor is missing or an unregistered editor appears.

## See also

- [#1129](https://github.com/laqieer/FEBuilderGBA/issues/1129) — decomp project mode tracker
- [#1150](https://github.com/laqieer/FEBuilderGBA/issues/1150) — round-trip audit / NMM bridge / asset validation
- [README → Decomp Project Support](../README.md#decomp-project-support-preview)
- [docs/cli-reference.md](cli-reference.md)
